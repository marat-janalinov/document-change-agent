# ✅ Чеклист критичных файлов для развертывания

## 📋 Файлы, не попадающие в репозиторий, но критичные для функционала

### 🔴 Критичные (обязательно нужны для работы)

1. **`.env`** - файл с переменными окружения
   - Содержит: `OPENAI_API_KEY`, пароли БД, JWT секреты
   - Без него: проект не запустится
   - Создание: вручную на сервере (см. `MANUAL_DEPLOYMENT.md`)

### 🟡 Важные (могут влиять на функционал)

2. **`data/prompts/*.md`** - модифицированные промпты для LLM
   - В репозитории: `backend/prompts/*.md` (резервная копия)
   - Автоматически: копируются из `backend/prompts/` при первом запуске
   - Если изменены локально: нужно скопировать вручную на сервер
   - Могут влиять на: качество парсинга инструкций, поведение LLM

3. **`mcp-server/Office-Word-MCP-Server/`** - исходники MCP сервера
   - ✅ Теперь в репозитории (добавлено в последнем коммите)
   - Могут влиять на: совместимость с разными версиями MCP сервера
   - Если отсутствуют: MCP сервер не соберется или будет работать нестабильно

### 🟢 Опциональные (не критичны для запуска)

4. **`data/uploads/`** - загруженные пользователями файлы
   - Не в репозитории (по дизайну)
   - Создается автоматически
   - Влияет только на: наличие пользовательских документов

5. **`data/outputs/`**, **`data/backups/`**, **`data/logs/`**
   - Не в репозитории
   - Создаются автоматически
   - Не критичны для запуска

6. **`docker-compose.override.yml`**
   - Не в репозитории (по дизайну)
   - Только для локальной разработки
   - Не нужен на production сервере

---

## 🔍 Проверка различий между локальной и удаленной средой

### Проверка 1: Версия MCP сервера

```bash
# На локальной машине
ls -la mcp-server/Office-Word-MCP-Server/word_document_server/tools/content_tools.py

# На сервере (в контейнере)
docker compose exec mcp-server cat word_document_server/tools/content_tools.py | grep "def search_and_replace"
```

**Проверьте:**
- Принимает ли функция `paragraph_index` как параметр?
- Совпадают ли сигнатуры функций?

### Проверка 2: Промпты

```bash
# На локальной машине
md5sum data/prompts/*.md

# На сервере
docker compose exec backend md5sum /data/prompts/*.md
```

**Проверьте:**
- Совпадают ли хеши файлов промптов?
- Есть ли дополнительные файлы промптов на сервере?

### Проверка 3: Переменные окружения

```bash
# На локальной машине (проверьте значения, не показывайте секреты!)
grep -v "PASSWORD\|SECRET\|KEY" .env

# На сервере
docker compose exec backend env | grep -v "PASSWORD\|SECRET\|KEY" | sort
```

**Проверьте:**
- Совпадают ли значения `OPENAI_MODEL`, `MCP_SERVER_HOST`, `MCP_SERVER_PORT`?
- Указаны ли правильные хосты для Docker сети?

### Проверка 4: Версии зависимостей

```bash
# На локальной машине (если установлены локально)
pip list | grep -E "openai|httpx|fastmcp|parlant"

# На сервере (в контейнере)
docker compose exec backend pip list | grep -E "openai|httpx|fastmcp|parlant"
```

**Проверьте:**
- Совпадают ли версии критичных библиотек?

---

## 🎯 Типичные причины различий в поведении

### 1. Разные версии MCP сервера
**Симптомы:** Ошибки валидации аргументов, неподдерживаемые параметры
**Решение:** ✅ Исправлено - исходники MCP сервера теперь в репозитории

### 2. Разные промпты
**Симптомы:** LLM возвращает разные форматы ответов
**Решение:** Синхронизировать `data/prompts/` или использовать версии из `backend/prompts/`

### 3. Разные версии библиотек
**Симптомы:** Несовместимость API, разные ошибки
**Решение:** Проверить `requirements.txt` и пересобрать образы

### 4. Разные переменные окружения
**Симптомы:** Разное поведение, разные настройки
**Решение:** Синхронизировать `.env` (кроме секретов)

### 5. Разные версии Python/Docker
**Симптомы:** Ошибки выполнения, проблемы с зависимостями
**Решение:** Использовать одинаковые версии на локальной и удаленной среде

---

## ✅ Чеклист перед развертыванием

- [ ] Все исходники из репозитория синхронизированы (git pull)
- [ ] Файл `.env` создан на сервере с правильными значениями
- [ ] Промпты скопированы (если были изменены локально)
- [ ] Структура директорий `data/` создана
- [ ] Docker и Docker Compose установлены на сервере
- [ ] Порты 8000, 8080, 9000 свободны на сервере
- [ ] Доступ к OpenAI API работает с сервера

---

## 📝 Быстрая команда для проверки критичных файлов

```bash
# На сервере после развертывания
cd ~/document-change-agent

echo "=== Проверка критичных файлов ==="
echo "1. .env файл:"
[ -f .env ] && echo "   ✅ Существует" || echo "   ❌ ОТСУТСТВУЕТ!"

echo "2. MCP сервер исходники:"
[ -d mcp-server/Office-Word-MCP-Server ] && echo "   ✅ Существует" || echo "   ❌ ОТСУТСТВУЕТ!"

echo "3. Промпты в backend:"
[ -d backend/prompts ] && echo "   ✅ Существует ($(ls backend/prompts/*.md 2>/dev/null | wc -l) файлов)" || echo "   ❌ ОТСУТСТВУЕТ!"

echo "4. Структура data/:"
[ -d data/prompts ] && echo "   ✅ data/prompts существует" || echo "   ❌ ОТСУТСТВУЕТ!"
[ -d data/uploads ] && echo "   ✅ data/uploads существует" || echo "   ⚠️  Будет создана автоматически"

echo "5. Docker Compose:"
[ -f docker-compose.yml ] && echo "   ✅ Существует" || echo "   ❌ ОТСУТСТВУЕТ!"

echo "=== Проверка завершена ==="
```

