# 🔄 Удаление существующего Git репозитория и создание нового

## Содержание

1. [Проверка текущего состояния](#проверка-текущего-состояния)
2. [Удаление существующего репозитория](#удаление-существующего-репозитория)
3. [Создание нового репозитория](#создание-нового-репозитория)
4. [Подключение к GitHub](#подключение-к-github)
5. [Первый коммит и push](#первый-коммит-и-push)
6. [Альтернативные методы](#альтернативные-методы)

---

## Проверка текущего состояния

### 1. Проверка наличия Git репозитория

```bash
# Переход в директорию проекта
cd /path/to/document-change-agent

# Проверка наличия .git директории
ls -la | grep .git

# Или проверка статуса Git
git status
```

Если команда `git status` выдает ошибку "not a git repository", значит репозиторий не инициализирован, и вы можете сразу перейти к [созданию нового репозитория](#создание-нового-репозитория).

### 2. Проверка текущих remote репозиториев

```bash
# Просмотр подключенных remote репозиториев
git remote -v

# Пример вывода:
# origin  https://github.com/old-username/old-repo.git (fetch)
# origin  https://github.com/old-username/old-repo.git (push)
```

### 3. Проверка текущей ветки и коммитов

```bash
# Текущая ветка
git branch

# История коммитов
git log --oneline -5
```

---

## Удаление существующего репозитория

### Метод 1: Полное удаление (рекомендуется)

Этот метод полностью удаляет всю историю Git и создает новый репозиторий с нуля.

```bash
# Переход в директорию проекта
cd /path/to/document-change-agent

# ⚠️ ВАЖНО: Убедитесь, что вы сохранили все важные изменения!
# Если есть несохраненные изменения, которые нужно сохранить:
git stash  # Сохраняет изменения во временное хранилище

# Удаление .git директории (это удалит всю историю Git)
rm -rf .git

# Проверка, что .git удалена
ls -la | grep .git
# Должно быть пусто (ничего не выводится)
```

### Метод 2: Удаление только remote (если нужно только изменить подключение)

Если вы хотите сохранить локальную историю коммитов, но изменить подключение к GitHub:

```bash
# Удаление существующего remote
git remote remove origin

# Или если remote называется по-другому
git remote remove <remote-name>

# Проверка
git remote -v
# Должно быть пусто
```

---

## Создание нового репозитория

### 1. Инициализация нового Git репозитория

```bash
# Убедитесь, что вы в директории проекта
cd /path/to/document-change-agent

# Инициализация нового репозитория
git init

# Проверка
git status
# Должно показать список неотслеживаемых файлов
```

### 2. Проверка .gitignore

```bash
# Проверка наличия .gitignore
cat .gitignore

# Если .gitignore отсутствует или неполный, создайте/обновите его
# (см. инструкцию в GITHUB_DEPLOYMENT.md)
```

### 3. Настройка Git (если еще не настроен)

```bash
# Настройка имени пользователя
git config user.name "Ваше Имя"

# Настройка email
git config user.email "your.email@example.com"

# Проверка настроек
git config --list
```

---

## Подключение к GitHub

### 1. Создание репозитория на GitHub

Если вы еще не создали репозиторий на GitHub:

1. Перейдите на https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `document-change-agent`
   - **Description:** (опционально)
   - **Visibility:** Public или Private
   - **НЕ** отмечайте "Initialize this repository with a README"
4. Нажмите **"Create repository"**
5. Скопируйте URL репозитория (HTTPS или SSH)

### 2. Добавление remote репозитория

```bash
# Добавление remote (замените URL на ваш)
git remote add origin https://github.com/yourusername/document-change-agent.git

# Или для SSH:
git remote add origin git@github.com:yourusername/document-change-agent.git

# Проверка
git remote -v
```

---

## Первый коммит и push

### 1. Добавление файлов

```bash
# Проверка статуса
git status

# Добавление всех файлов (кроме указанных в .gitignore)
git add .

# Проверка добавленных файлов
git status
```

### 2. Проверка перед коммитом

```bash
# ⚠️ ВАЖНО: Убедитесь, что .env файл НЕ добавлен!
git status | grep .env

# Если .env попал в staging, удалите его:
git reset HEAD .env

# Проверка, что секреты не попадут в репозиторий
git status | grep -E "\.env|secrets|\.key|\.pem"
```

### 3. Создание первого коммита

```bash
# Создание коммита
git commit -m "Initial commit: Document Change Agent project"

# Или более подробное сообщение
git commit -m "Initial commit

- Add Docker Compose configuration
- Add FastAPI backend with Parlant Agent
- Add React frontend
- Add MCP Word Server integration
- Add documentation and deployment guides"
```

### 4. Переименование ветки (если нужно)

```bash
# Переименование в main (если используется master)
git branch -M main

# Проверка текущей ветки
git branch
```

### 5. Push на GitHub

```bash
# Push в репозиторий (первый раз)
git push -u origin main

# Если ветка называется master:
git push -u origin master

# В дальнейшем можно использовать просто:
git push
```

### 6. Проверка на GitHub

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что `.env` файл **НЕ** загружен

---

## Альтернативные методы

### Метод A: Клонирование и замена файлов

Если вы хотите начать с чистого репозитория на GitHub:

```bash
# 1. Создайте репозиторий на GitHub (с README)

# 2. Клонируйте его
cd /path/to
git clone https://github.com/yourusername/document-change-agent.git
cd document-change-agent

# 3. Удалите все файлы из клонированного репозитория (кроме .git)
rm -rf * .[!.]* 2>/dev/null || true

# 4. Скопируйте файлы вашего проекта
cp -r /path/to/your/project/* .
cp -r /path/to/your/project/.[!.]* . 2>/dev/null || true

# 5. Убедитесь, что .gitignore настроен правильно
# 6. Добавьте файлы и сделайте коммит
git add .
git commit -m "Initial commit"
git push
```

### Метод B: Сохранение истории, но изменение remote

Если вы хотите сохранить локальную историю коммитов:

```bash
# 1. Удалите только remote
git remote remove origin

# 2. Добавьте новый remote
git remote add origin https://github.com/yourusername/new-repo.git

# 3. Push с force (если нужно перезаписать историю на GitHub)
git push -u origin main --force

# ⚠️ Осторожно с --force! Используйте только если уверены.
```

### Метод C: Создание новой ветки и удаление старой

```bash
# 1. Создайте новую ветку без истории
git checkout --orphan new-main

# 2. Добавьте все файлы
git add .

# 3. Создайте первый коммит
git commit -m "Initial commit"

# 4. Удалите старую ветку
git branch -D main  # или master

# 5. Переименуйте новую ветку
git branch -m main

# 6. Push
git push -u origin main --force
```

---

## Полная последовательность команд (быстрый старт)

Если вы хотите полностью пересоздать репозиторий:

```bash
# 1. Переход в директорию проекта
cd /path/to/document-change-agent

# 2. Удаление существующего репозитория
rm -rf .git

# 3. Инициализация нового репозитория
git init

# 4. Настройка Git (если нужно)
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"

# 5. Проверка .gitignore
cat .gitignore  # Убедитесь, что .env в списке

# 6. Добавление файлов
git add .

# 7. Проверка, что .env не добавлен
git status | grep .env  # Должно быть пусто

# 8. Создание первого коммита
git commit -m "Initial commit: Document Change Agent project"

# 9. Переименование ветки (если нужно)
git branch -M main

# 10. Добавление remote (замените URL)
git remote add origin https://github.com/yourusername/document-change-agent.git

# 11. Проверка remote
git remote -v

# 12. Push на GitHub
git push -u origin main
```

---

## Решение проблем

### Проблема: "remote origin already exists"

```bash
# Удаление существующего remote
git remote remove origin

# Добавление нового
git remote add origin https://github.com/yourusername/new-repo.git
```

### Проблема: "failed to push some refs"

```bash
# Если на GitHub уже есть коммиты, которые нужно перезаписать:
git push -u origin main --force

# ⚠️ Используйте --force только если уверены, что хотите перезаписать историю на GitHub
```

### Проблема: "Permission denied"

```bash
# Проверка прав доступа
# Для HTTPS: убедитесь, что используете Personal Access Token
# Для SSH: проверьте настройку SSH ключей

# Проверка SSH подключения
ssh -T git@github.com

# Если нужно настроить SSH ключи:
# https://docs.github.com/en/authentication/connecting-to-github-with-ssh
```

### Проблема: Случайно удалили .git, но нужна история

```bash
# Если вы только что удалили .git, но еще не сделали коммит:
# История может быть восстановлена через reflog (если Git еще помнит)

# Но если вы уже сделали git init, история потеряна
# В этом случае нужно восстановить из резервной копии или GitHub
```

---

## Чеклист

Перед удалением существующего репозитория убедитесь:

- [ ] Все важные изменения сохранены
- [ ] Вы сделали backup важных данных
- [ ] Вы знаете URL нового репозитория на GitHub
- [ ] .gitignore настроен правильно
- [ ] .env файл не будет загружен
- [ ] Вы готовы потерять локальную историю Git (если используете Метод 1)

После создания нового репозитория проверьте:

- [ ] Git репозиторий инициализирован (`git status` работает)
- [ ] Remote добавлен (`git remote -v` показывает ваш репозиторий)
- [ ] Файлы добавлены (`git status` показывает staged файлы)
- [ ] Первый коммит создан (`git log` показывает коммит)
- [ ] Push выполнен успешно (файлы видны на GitHub)
- [ ] .env файл НЕ загружен на GitHub

---

## Полезные команды для проверки

```bash
# Проверка статуса
git status

# Просмотр истории
git log --oneline

# Просмотр remote
git remote -v

# Просмотр веток
git branch -a

# Просмотр изменений
git diff

# Проверка, что файл в .gitignore
git check-ignore -v .env
```

---

**Версия инструкции:** 1.0  
**Дата обновления:** 2025-11-24

---

*Успешной работы с Git! 🚀*

