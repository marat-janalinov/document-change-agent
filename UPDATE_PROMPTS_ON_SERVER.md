# 🔄 СРОЧНОЕ ОБНОВЛЕНИЕ ПРОМПТОВ НА СЕРВЕРЕ

## ⚠️ ПРОБЛЕМА

Судя по логам, на сервере все еще используются старые промпты. LLM возвращает один объект вместо массива `{"changes": [...]}`, что приводит к тому, что распознается только одно изменение вместо всех.

**Признаки проблемы в логах:**
- LLM возвращает: `{"change_id": "CHG-001", ...}` вместо `{"changes": [...]}`
- "Найдено ключевых слов инструкций в тексте: 3"
- "LLM распознал изменений: 1"

## ✅ РЕШЕНИЕ - БЫСТРОЕ ОБНОВЛЕНИЕ

### Вариант 1: Автоматическое обновление (РЕКОМЕНДУЕТСЯ)

```bash
# На удаленном сервере
cd /path/to/document-change-agent

# 1. Обновить код из репозитория
git pull origin main

# 2. Удалить старые промпты (чтобы гарантировать обновление)
rm -f data/prompts/*.md

# 3. Перезапустить backend (он скопирует новые промпты)
docker-compose restart backend

# 4. Проверить логи
docker-compose logs -f backend | grep -i "промпт\|prompt\|changes"
```

### Вариант 2: Ручное копирование промптов

Если нужно обновить без перезапуска:

```bash
# На удаленном сервере
cd /path/to/document-change-agent

# 1. Обновить код
git pull origin main

# 2. Скопировать промпты вручную
cp backend/prompts/*.md data/prompts/

# 3. Проверить, что файлы скопировались
ls -lh data/prompts/
wc -l data/prompts/*.md
```

**Ожидаемые размеры файлов после обновления:**
- `instruction_check_system.md`: **~205 строк** (было ~170)
- `instruction_check_user.md`: **~210 строк** (было ~164)

### Вариант 3: Через init_prompts.py

```bash
# Войти в контейнер backend
docker-compose exec backend bash

# Запустить скрипт инициализации
python -c "from init_prompts import init_prompts; init_prompts()"

# Выйти
exit
```

## 🔍 ПРОВЕРКА УСПЕШНОГО ОБНОВЛЕНИЯ

### 1. Проверить версии файлов:

```bash
# На сервере
cd /path/to/document-change-agent
wc -l data/prompts/*.md
```

**Должны быть большие файлы:**
- `instruction_check_system.md`: **205+ строк**
- `instruction_check_user.md`: **210+ строк**

Если меньше - промпты не обновились.

### 2. Проверить содержимое промпта:

```bash
# На сервере
grep -i "КРИТИЧЕСКИ ВАЖНО" data/prompts/instruction_check_system.md | head -3
```

**Должно быть:**
```
**КРИТИЧЕСКИ ВАЖНО**: Ответ ДОЛЖЕН быть в формате `{"changes": [...]}`...
```

### 3. Проверить логи при следующей проверке инструкций:

После обновления промптов, при следующей проверке инструкций в логах должно быть:

```
📤 System prompt (первые 500 символов): ...
📤 User prompt (первые 500 символов): ...
```

И LLM должен вернуть:
```json
{
  "changes": [
    {"change_id": "CHG-001", ...},
    {"change_id": "CHG-002", ...},
    ...
  ]
}
```

## 🚨 ЕСЛИ ПРОБЛЕМА СОХРАНЯЕТСЯ

Если после обновления промптов LLM все еще возвращает один объект:

1. **Проверить логи промптов:**
```bash
docker-compose logs backend | grep "📤 System prompt"
```

2. **Убедиться, что промпты действительно обновились:**
```bash
grep -c "КРИТИЧЕСКИ ВАЖНО.*changes" data/prompts/instruction_check_system.md
```

Должно быть минимум 2 совпадения.

3. **Проверить, что код обновился:**
```bash
grep -c "📤 System prompt" backend/parlant_agent.py
```

Должно быть 1 (один раз в коде).

4. **Пересоздать контейнер полностью:**
```bash
docker-compose down
docker-compose up --build -d
```

## 📝 ЧТО ИЗМЕНИЛОСЬ В ПРОМПТАХ

1. **Добавлены явные инструкции** о формате `{"changes": [...]}` в начало раздела о структуре JSON
2. **Добавлены примеры** правильного и неправильного формата
3. **Усилены предупреждения** о том, что ВСЕ найденные изменения должны быть в массиве
4. **Добавлено логирование** промптов для диагностики

Эти изменения должны заставить LLM возвращать правильный формат.

