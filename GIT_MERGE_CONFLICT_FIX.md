# 🔧 Исправление конфликта git merge на сервере

**Дата:** 2025-11-24  
**Проблема:** Локальные изменения в `mcp-server/Dockerfile` конфликтуют с обновлениями из репозитория

---

## ✅ Решение

### Вариант 1: Отменить локальные изменения и взять версию из репозитория (РЕКОМЕНДУЕТСЯ)

**На сервере выполните:**

```bash
cd ~/document-change-agent

# Отменить локальные изменения в Dockerfile
git checkout -- mcp-server/Dockerfile

# Теперь можно сделать pull
git pull origin main

# Проверить, что Dockerfile обновлен
cat mcp-server/Dockerfile | grep -A 2 "trusted-host"
```

---

### Вариант 2: Сохранить локальные изменения (если они важны)

**На сервере выполните:**

```bash
cd ~/document-change-agent

# Сохранить локальные изменения во временное хранилище
git stash

# Получить обновления
git pull origin main

# Посмотреть сохраненные изменения
git stash show -p

# Если нужно применить сохраненные изменения поверх новых
git stash pop

# Или отбросить сохраненные изменения
git stash drop
```

---

### Вариант 3: Закоммитить локальные изменения

**На сервере выполните:**

```bash
cd ~/document-change-agent

# Посмотреть, что изменилось
git diff mcp-server/Dockerfile

# Если изменения нужны, закоммитить
git add mcp-server/Dockerfile
git commit -m "fix: Локальные изменения Dockerfile на сервере"

# Теперь можно сделать pull (может быть merge конфликт)
git pull origin main

# Если есть конфликт, разрешить его вручную
# Затем:
git add mcp-server/Dockerfile
git commit -m "fix: Разрешен конфликт в Dockerfile"
```

---

## 🎯 Рекомендуемый порядок действий

**На сервере:**

```bash
cd ~/document-change-agent

# 1. Посмотреть, что изменилось локально
git diff mcp-server/Dockerfile

# 2. Если локальные изменения не нужны (скорее всего так и есть)
git checkout -- mcp-server/Dockerfile

# 3. Получить обновления
git pull origin main

# 4. Проверить, что Dockerfile правильный
cat mcp-server/Dockerfile

# 5. Пересобрать образ
docker compose build --no-cache mcp-server
```

---

## ⚠️ Важно

Если на сервере была старая версия Dockerfile (без `--trusted-host`), то **лучше отменить локальные изменения** и взять правильную версию из репозитория, где уже есть все исправления.

---

**Версия:** 1.0  
**Дата:** 2025-11-24

