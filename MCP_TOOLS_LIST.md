# Полный список инструментов MCP Word Server

## Всего доступно: 54 инструмента

---

## 📋 Категории инструментов

### 1. Управление документами (Document Management) - 7 инструментов

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 1 | `create_document` | Создание нового Word документа с метаданными | ❌ НЕТ |
| 2 | `copy_document` | Создание копии документа | ✅ **ДА** (parlant_agent.py:443) |
| 3 | `get_document_info` | Получение информации о документе | ❌ НЕТ |
| 4 | `get_document_text` | Извлечение всего текста из документа | ✅ **ДА** (parlant_agent.py:447, app.py:420, 498) |
| 5 | `get_document_outline` | Получение структуры документа | ❌ НЕТ |
| 6 | `get_document_xml` | Получение XML структуры документа | ❌ НЕТ |
| 7 | `list_available_documents` | Список всех .docx файлов в директории | ❌ НЕТ |

### 2. Поиск и чтение (Search & Read) - 2 инструмента

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 8 | `find_text_in_document` | Поиск текста в документе | ✅ **ДА** (множество мест в parlant_agent.py) |
| 9 | `get_paragraph_text_from_document` | Получение текста конкретного параграфа | ❌ НЕТ |

### 3. Создание контента (Content Creation) - 10 инструментов

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 10 | `add_paragraph` | Добавление параграфа | ⚠️ **ЧАСТИЧНО** (только если position=None) |
| 11 | `add_heading` | Добавление заголовка | ⚠️ **ЧАСТИЧНО** (только если position=None) |
| 12 | `add_picture` | Добавление изображения | ❌ НЕТ |
| 13 | `add_table` | Создание таблицы | ❌ НЕТ |
| 14 | `add_page_break` | Вставка разрыва страницы | ❌ НЕТ |
| 15 | `insert_header_near_text` | Вставка заголовка рядом с текстом | ❌ НЕТ |
| 16 | `insert_line_or_paragraph_near_text` | Вставка строки/параграфа рядом с текстом | ❌ НЕТ |
| 17 | `insert_numbered_list_near_text` | Вставка нумерованного/маркированного списка | ❌ НЕТ |
| 18 | `replace_paragraph_block_below_header` | Замена блока параграфов под заголовком | ❌ НЕТ |
| 19 | `replace_block_between_manual_anchors` | Замена блока между якорями | ❌ НЕТ |

### 4. Модификация контента (Content Modification) - 2 инструмента

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 20 | `search_and_replace` | Поиск и замена текста | ❌ **НЕТ** (используется python-docx напрямую) |
| 21 | `delete_paragraph` | Удаление параграфа | ❌ **НЕТ** (используется python-docx напрямую) |

### 5. Форматирование текста (Text Formatting) - 2 инструмента

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 22 | `format_text` | Форматирование текста (жирный, курсив, цвет и т.д.) | ❌ НЕТ |
| 23 | `create_custom_style` | Создание пользовательского стиля | ❌ НЕТ |

### 6. Работа с таблицами (Table Operations) - 15 инструментов

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 24 | `format_table` | Форматирование таблицы (границы, стили) | ❌ НЕТ |
| 25 | `format_table_cell_text` | Форматирование текста в ячейке таблицы | ❌ НЕТ |
| 26 | `set_table_cell_shading` | Установка заливки ячейки | ❌ НЕТ |
| 27 | `apply_table_alternating_rows` | Чередующиеся цвета строк | ❌ НЕТ |
| 28 | `highlight_table_header` | Выделение заголовка таблицы | ❌ НЕТ |
| 29 | `merge_table_cells` | Объединение ячеек таблицы | ❌ НЕТ |
| 30 | `merge_table_cells_horizontal` | Горизонтальное объединение ячеек | ❌ НЕТ |
| 31 | `merge_table_cells_vertical` | Вертикальное объединение ячеек | ❌ НЕТ |
| 32 | `set_table_cell_alignment` | Выравнивание ячейки | ❌ НЕТ |
| 33 | `set_table_alignment_all` | Выравнивание всех ячеек | ❌ НЕТ |
| 34 | `set_table_cell_padding` | Отступы в ячейке | ❌ НЕТ |
| 35 | `set_table_column_width` | Ширина колонки | ❌ НЕТ |
| 36 | `set_table_column_widths` | Ширины всех колонок | ❌ НЕТ |
| 37 | `set_table_width` | Ширина таблицы | ❌ НЕТ |
| 38 | `auto_fit_table_columns` | Автоподбор ширины колонок | ❌ НЕТ |

### 7. Работа с комментариями (Comments) - 3 инструмента

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 39 | `get_all_comments` | Получение всех комментариев | ❌ НЕТ |
| 40 | `get_comments_by_author` | Получение комментариев по автору | ❌ НЕТ |
| 41 | `get_comments_for_paragraph` | Получение комментариев для параграфа | ❌ НЕТ |

**Примечание:** `add_comment` НЕ доступен как MCP tool - реализован локально через python-docx

### 8. Работа с сносками (Footnotes & Endnotes) - 8 инструментов

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 42 | `add_footnote_to_document` | Добавление сноски к документу | ❌ НЕТ |
| 43 | `add_footnote_after_text` | Добавление сноски после текста | ❌ НЕТ |
| 44 | `add_footnote_before_text` | Добавление сноски перед текстом | ❌ НЕТ |
| 45 | `add_footnote_enhanced` | Расширенное добавление сноски | ❌ НЕТ |
| 46 | `add_footnote_robust` | Надежное добавление сноски | ❌ НЕТ |
| 47 | `add_endnote_to_document` | Добавление концевых сносок | ❌ НЕТ |
| 48 | `delete_footnote_from_document` | Удаление сноски | ❌ НЕТ |
| 49 | `delete_footnote_robust` | Надежное удаление сноски | ❌ НЕТ |
| 50 | `customize_footnote_style` | Настройка стиля сносок | ❌ НЕТ |
| 51 | `validate_document_footnotes` | Валидация сносок в документе | ❌ НЕТ |

**Примечание:** В коде есть `convert_footnotes_to_endnotes_in_document`, но он не в списке зарегистрированных

### 9. Защита документов (Document Protection) - 2 инструмента

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 52 | `protect_document` | Защита документа паролем | ❌ НЕТ |
| 53 | `unprotect_document` | Снятие защиты документа | ❌ НЕТ |

### 10. Конвертация (Conversion) - 1 инструмент

| № | Инструмент | Описание | Используется? |
|---|-----------|----------|---------------|
| 54 | `convert_to_pdf` | Конвертация Word в PDF | ❌ НЕТ |

---

## 📊 Статистика использования

### Используется в коде:
- ✅ **3 инструмента** активно используются:
  1. `get_document_text` - чтение файлов инструкций
  2. `find_text_in_document` - поиск текста перед применением изменений
  3. `copy_document` - создание резервных копий

- ⚠️ **2 инструмента** используются частично:
  4. `add_paragraph` - только если position=None
  5. `add_heading` - только если position=None

### НЕ используется в коде:
- ❌ **49 инструментов** не используются

---

## 🔍 Детальный анализ использования

### Активно используемые инструменты:

#### 1. `get_document_text`
```python
# Использование:
changes_text = await mcp_client.get_document_text(changes_file)

# Где используется:
- parlant_agent.py:447 (извлечение инструкций)
- app.py:420, 498 (проверка инструкций)
```

#### 2. `find_text_in_document`
```python
# Использование:
matches = await mcp_client.find_text_in_document(
    filename,
    normalized_target,
    match_case=match_case,
)

# Где используется:
- parlant_agent.py:663, 672, 684 (REPLACE_TEXT)
- parlant_agent.py:808, 817 (DELETE_PARAGRAPH)
- parlant_agent.py:899, 907, 916 (REPLACE_POINT_TEXT)
- parlant_agent.py:989, 997 (INSERT_PARAGRAPH)
- parlant_agent.py:1057 (INSERT_SECTION)
- parlant_agent.py:1121 (ADD_COMMENT)
```

#### 3. `copy_document`
```python
# Использование:
await mcp_client.copy_document(source_file, backup_path)

# Где используется:
- parlant_agent.py:443 (создание резервной копии)
```

---

## 🚫 Инструменты, которые НЕ используются, но могли бы быть полезны:

### Для замены текста:
- `search_and_replace` - **НЕ используется**, вместо этого используется python-docx напрямую

### Для удаления:
- `delete_paragraph` - **НЕ используется**, вместо этого используется python-docx напрямую

### Для вставки:
- `add_paragraph` - используется только частично (если position=None)
- `insert_line_or_paragraph_near_text` - **НЕ используется**, но мог бы быть полезен для INSERT_PARAGRAPH

### Для работы с комментариями:
- `get_all_comments` - **НЕ используется**, но мог бы быть полезен для анализа
- `get_comments_by_author` - **НЕ используется**
- `get_comments_for_paragraph` - **НЕ используется**

---

## 💡 Рекомендации по использованию

### Можно заменить прямые вызовы python-docx на MCP:

1. **`search_and_replace`** вместо прямого `Document(filename)`:
   ```python
   # Вместо:
   doc = Document(filename)
   # ... замена текста
   doc.save(filename)
   
   # Можно использовать:
   await mcp_client.replace_text(filename, old_text, new_text, paragraph_index)
   ```

2. **`delete_paragraph`** вместо прямого удаления:
   ```python
   # Вместо:
   doc = Document(filename)
   self._delete_paragraph(doc.paragraphs[index])
   doc.save(filename)
   
   # Можно использовать:
   await mcp_client.delete_paragraph(filename, paragraph_index)
   ```

3. **`insert_line_or_paragraph_near_text`** для INSERT_PARAGRAPH:
   ```python
   # Вместо сложной логики поиска и вставки:
   # Можно использовать:
   await mcp_client.insert_line_or_paragraph_near_text(
       filename, 
       target_text=after_text,
       line_text=new_text,
       position='after'
   )
   ```

---

## 📝 Итоговая таблица использования

| Категория | Всего | Используется | Не используется |
|-----------|-------|--------------|-----------------|
| Управление документами | 7 | 2 | 5 |
| Поиск и чтение | 2 | 1 | 1 |
| Создание контента | 10 | 0 (частично 2) | 8 |
| Модификация контента | 2 | 0 | 2 |
| Форматирование | 2 | 0 | 2 |
| Таблицы | 15 | 0 | 15 |
| Комментарии | 3 | 0 | 3 |
| Сноски | 10 | 0 | 10 |
| Защита | 2 | 0 | 2 |
| Конвертация | 1 | 0 | 1 |
| **ИТОГО** | **54** | **3-5** | **49-51** |

---

## 🔗 Связь с MCPClient

Все доступные инструменты можно вызвать через `MCPClient._call_tool()`:

```python
# Пример вызова любого инструмента:
result = await mcp_client._call_tool(
    "tool_name",
    {"param1": "value1", "param2": "value2"},
    expect_json=False
)
```

Но в коде используются только обертки:
- `mcp_client.get_document_text()` → `get_document_text`
- `mcp_client.find_text_in_document()` → `find_text_in_document`
- `mcp_client.copy_document()` → `copy_document`
- `mcp_client.replace_text()` → `search_and_replace` (но не используется)
- `mcp_client.delete_paragraph()` → `delete_paragraph` (но не используется)
- `mcp_client.add_paragraph()` → `add_paragraph` (частично)
- `mcp_client.add_heading()` → `add_heading` (частично)

---

## 📚 Источник

MCP Word Server: [Office-Word-MCP-Server by GongRzhe](https://github.com/GongRzhe/Office-Word-MCP-Server)

Все инструменты регистрируются в `/app/word_document_server/main.py` через декоратор `@mcp.tool()`.

