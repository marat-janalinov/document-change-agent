# 📦 Ручное развертывание проекта на удаленном сервере из локальной директории

## ⚠️ Критичные файлы, не попадающие в репозиторий

### 1. **Файл `.env`** (КРИТИЧНО!)
- Содержит секретные ключи и настройки окружения
- Должен быть создан на сервере вручную
- **Без этого файла проект не запустится!**

### 2. **Директория `data/prompts/`** (опционально)
- Промпты для LLM могут быть модифицированы локально
- Автоматически копируются из `backend/prompts/` при первом запуске
- Если есть локальные изменения, их нужно скопировать вручную

### 3. **Пользовательские файлы `data/uploads/`** (опционально)
- Загруженные пользователями документы
- Не критично для первого запуска
- Создаются автоматически

---

## 📋 Пошаговая инструкция ручного развертывания

### Шаг 1: Подготовка локальной директории

```bash
# На локальной машине перейдите в директорию проекта
cd ~/Downloads/document-change-agent

# Создайте архив проекта (исключая ненужные файлы)
tar -czf document-change-agent.tar.gz \
    --exclude='.git' \
    --exclude='node_modules' \
    --exclude='data/uploads' \
    --exclude='data/outputs' \
    --exclude='data/backups' \
    --exclude='data/logs' \
    --exclude='.env' \
    --exclude='.env.*' \
    --exclude='*.pyc' \
    --exclude='__pycache__' \
    --exclude='.DS_Store' \
    --exclude='*.log' \
    --exclude='react/node_modules' \
    --exclude='react/.env' \
    --exclude='react/.env.*' \
    .
```

**Альтернатива:** Используйте `rsync` для прямой синхронизации:

```bash
rsync -avz --progress \
    --exclude='.git' \
    --exclude='node_modules' \
    --exclude='data/uploads' \
    --exclude='data/outputs' \
    --exclude='data/backups' \
    --exclude='data/logs' \
    --exclude='.env' \
    --exclude='.env.*' \
    --exclude='*.pyc' \
    --exclude='__pycache__' \
    --exclude='.DS_Store' \
    --exclude='*.log' \
    ~/Downloads/document-change-agent/ \
    user@your-server-ip:~/document-change-agent/
```

---

### Шаг 2: Передача файлов на сервер

#### Вариант A: Через SCP (если создали архив)

```bash
# Передача архива на сервер
scp document-change-agent.tar.gz user@your-server-ip:~/

# Подключение к серверу
ssh user@your-server-ip

# Распаковка архива
cd ~
tar -xzf document-change-agent.tar.gz -C document-change-agent/
cd document-change-agent
```

#### Вариант B: Через rsync (прямая синхронизация)

```bash
# Выполняется на локальной машине
rsync -avz --progress \
    --exclude='.git' \
    --exclude='node_modules' \
    --exclude='data/uploads' \
    --exclude='data/outputs' \
    --exclude='data/backups' \
    --exclude='data/logs' \
    --exclude='.env' \
    --exclude='.env.*' \
    --exclude='*.pyc' \
    --exclude='__pycache__' \
    --exclude='.DS_Store' \
    --exclude='*.log' \
    ~/Downloads/document-change-agent/ \
    user@your-server-ip:~/document-change-agent/
```

---

### Шаг 3: Настройка на сервере

#### 3.1. Создание директорий

```bash
# На сервере
cd ~/document-change-agent

# Создание необходимых директорий
mkdir -p data/{uploads,outputs,backups,logs,prompts}

# Установка прав (для Docker volumes)
sudo chmod -R 777 data/
```

#### 3.2. Создание файла `.env`

```bash
# Создание файла .env из примера (если есть)
if [ -f .env.example ]; then
    cp .env.example .env
else
    # Создание нового файла .env
    cat > .env << 'EOF'
# OpenAI API Key (ОБЯЗАТЕЛЬНО - замените на ваш ключ!)
OPENAI_API_KEY=sk-your-openai-api-key-here

# База данных PostgreSQL
POSTGRES_DB=document_agent
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here

# JWT Secret Key (сгенерируйте случайный ключ)
JWT_SECRET_KEY=your-very-secure-random-secret-key-min-32-chars

# MCP Server настройки
MCP_SERVER_HOST=mcp-server
MCP_SERVER_PORT=8000

# Data directory
DATA_DIR=/data

# PostgreSQL connection
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:your-secure-password-here@postgres:5432/document_agent

# OpenAI настройки (опционально)
OPENAI_MODEL=gpt-4o
OPENAI_VERIFY_SSL=false
EOF
fi

# Редактирование файла .env
nano .env

# Установка прав на файл .env
chmod 600 .env
```

**⚠️ ВАЖНО:** Замените в файле `.env`:
- `sk-your-openai-api-key-here` на ваш реальный OpenAI API ключ
- `your-secure-password-here` на надежный пароль для PostgreSQL
- Сгенерируйте новый JWT Secret Key:

```bash
# Генерация случайного JWT Secret Key
openssl rand -hex 32
```

#### 3.3. Копирование промптов (ВАЖНО!)

⚠️ **КРИТИЧНО:** Промпты могут отличаться между локальной и удаленной средой!

**Проблема:** На удаленном сервере могут использоваться старые версии промптов из `data/prompts/`, которые не синхронизированы с новыми версиями из `backend/prompts/`.

**Решение:**

```bash
# На локальной машине - сначала убедитесь, что промпты синхронизированы
# Промпты в backend/prompts/ - это актуальные версии в репозитории
# Промпты в data/prompts/ - используются в runtime

# Синхронизируйте промпты локально (если еще не сделано)
cp backend/prompts/*.md data/prompts/

# Сравните версии на сервере и локально
# На локальной машине:
md5sum backend/prompts/*.md data/prompts/*.md

# Синхронизируйте промпты на сервер
scp backend/prompts/*.md user@your-server-ip:~/document-change-agent/data/prompts/

# Или если хотите использовать те же, что локально:
scp data/prompts/*.md user@your-server-ip:~/document-change-agent/data/prompts/

# На сервере проверьте, что файлы скопировались и совпадают
ssh user@your-server-ip
cd ~/document-change-agent
md5sum backend/prompts/*.md data/prompts/*.md
```

**Проверка различий промптов:**

```bash
# На локальной машине - проверьте размеры и даты файлов
ls -lh backend/prompts/ data/prompts/

# Если файлы отличаются по размеру или дате - они разные!
# Например, если instruction_check_system.md:
# - В backend/prompts/: 11KB (обновлен 28 ноября)
# - В data/prompts/: 3KB (старая версия от 24 ноября)
# → Это разные версии!
```

**Примечание:** При первом запуске на сервере промпты автоматически копируются из `backend/prompts/` в `data/prompts/`, но если файлы уже существуют в `data/prompts/`, они НЕ перезаписываются. Поэтому важно вручную синхронизировать промпты!

---

### Шаг 4: Проверка структуры проекта на сервере

```bash
# На сервере
cd ~/document-change-agent

# Проверка структуры
ls -la

# Должны быть видны:
# - docker-compose.yml
# - backend/
# - react/
# - mcp-server/
# - .env (файл должен существовать!)

# Проверка критичных файлов
ls -la mcp-server/Office-Word-MCP-Server/ | head -10
ls -la backend/prompts/
ls -la data/prompts/
```

---

### Шаг 5: Запуск проекта

```bash
# Остановка старых контейнеров (если запущены)
docker compose down

# Пересборка образов
docker compose build --no-cache

# Запуск контейнеров
docker compose up -d

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f
```

---

### Шаг 6: Проверка работоспособности

```bash
# Проверка health check backend
curl http://localhost:8000/health

# Проверка frontend
curl http://localhost:8080

# Проверка логов каждого сервиса
docker compose logs backend
docker compose logs react-frontend
docker compose logs mcp-server
docker compose logs postgres
```

---

## 🔄 Обновление проекта (при изменении локальных файлов)

### Быстрое обновление через rsync

```bash
# На локальной машине
rsync -avz --progress \
    --exclude='.git' \
    --exclude='node_modules' \
    --exclude='data/uploads' \
    --exclude='data/outputs' \
    --exclude='data/backups' \
    --exclude='data/logs' \
    --exclude='.env' \
    --exclude='.env.*' \
    --exclude='*.pyc' \
    --exclude='__pycache__' \
    --exclude='.DS_Store' \
    --exclude='*.log' \
    ~/Downloads/document-change-agent/ \
    user@your-server-ip:~/document-change-agent/

# На сервере перезапуск
ssh user@your-server-ip
cd ~/document-change-agent
docker compose down
docker compose build --no-cache
docker compose up -d
```

---

## 📝 Чеклист критичных файлов

Перед запуском проверьте наличие:

- [ ] `.env` - файл с настройками окружения
- [ ] `docker-compose.yml` - конфигурация Docker
- [ ] `backend/` - исходники backend
- [ ] `react/` - исходники frontend
- [ ] `mcp-server/Office-Word-MCP-Server/` - исходники MCP сервера
- [ ] `backend/prompts/` - промпты (в репозитории)
- [ ] `data/prompts/` - промпты (опционально, создаются автоматически)
- [ ] `data/` - директория для данных с подпапками

---

## 🆘 Решение проблем

### Проблема: Файл `.env` отсутствует

**Решение:**
```bash
# Создайте файл .env как описано в шаге 3.2
# Обязательно укажите OPENAI_API_KEY!
```

### Проблема: MCP сервер не запускается

**Решение:**
```bash
# Проверьте наличие исходников MCP сервера
ls -la mcp-server/Office-Word-MCP-Server/

# Если их нет, они должны быть в архиве/репозитории
# Если используете git, выполните:
git pull origin main
```

### Проблема: Промпты не загружаются

**Решение:**
```bash
# Проверьте наличие промптов в backend/prompts/
ls -la backend/prompts/

# Проверьте логи backend на предмет ошибок копирования промптов
docker compose logs backend | grep -i prompt
```

### Проблема: Контейнеры не собираются

**Решение:**
```bash
# Очистите кэш Docker
docker system prune -a

# Пересоберите без кэша
docker compose build --no-cache

# Проверьте логи сборки
docker compose build 2>&1 | tee build.log
```

---

## 🔐 Безопасность

⚠️ **ВАЖНО:** Никогда не передавайте файл `.env` через незащищенные каналы!

Рекомендуется:
1. Передавать `.env` через SSH (скопировать содержимое вручную)
2. Использовать переменные окружения вместо файла `.env` в production
3. Регулярно менять пароли и ключи
4. Ограничить доступ к файлу `.env` (`chmod 600 .env`)

---

## 📞 Дополнительная информация

После развертывания проект будет доступен:
- **Frontend:** `http://your-server-ip:8080`
- **Backend API:** `http://your-server-ip:8000`
- **Health Check:** `http://your-server-ip:8000/health`

Для production рекомендуется настроить Nginx как reverse proxy (см. `DEPLOYMENT_UBUNTU.md`).

