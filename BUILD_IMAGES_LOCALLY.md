# 🐳 Сборка Docker образов локально и передача на сервер

**Дата:** 2025-11-24  
**Цель:** Собрать все Docker образы локально и передать на сервер для обхода проблем с SSL/сетью

---

## ✅ Преимущества

1. **Обход проблем с SSL** - образы собираются в рабочем окружении
2. **Быстрее** - не нужно ждать сборки на сервере
3. **Надежнее** - можно проверить образы локально перед передачей
4. **Экономия трафика** - не нужно скачивать зависимости на сервере

---

## 📋 Пошаговая инструкция

### Шаг 1: Сборка образов локально

**На локальной машине:**

```bash
cd ~/Downloads/document-change-agent

# Собрать все образы
docker compose build

# Или собрать конкретные образы
docker compose build backend
docker compose build react-frontend
docker compose build mcp-server
docker compose build frontend  # если используется
```

**Проверка собранных образов:**
```bash
docker images | grep document-change-agent
```

---

### Шаг 2: Сохранение образов в файлы

**На локальной машине:**

```bash
# Создать директорию для сохранения образов
mkdir -p docker-images
cd docker-images

# Сохранить каждый образ
docker save document-change-agent-backend:latest -o backend.tar
docker save document-change-agent-react-frontend:latest -o react-frontend.tar
docker save document-change-agent-mcp-server:latest -o mcp-server.tar
docker save document-change-agent-frontend:latest -o frontend.tar  # если используется

# Также сохранить базовые образы, которые используются
docker save postgres:16-alpine -o postgres-16-alpine.tar
docker save nginx:alpine -o nginx-alpine.tar

# Проверить размеры файлов
ls -lh *.tar
```

**Или сохранить все образы проекта одной командой:**
```bash
# Получить список всех образов проекта
docker images --format "{{.Repository}}:{{.Tag}}" | grep document-change-agent > images-list.txt

# Сохранить все образы
while read image; do
    filename=$(echo $image | sed 's/:/_/g' | sed 's/\//_/g').tar
    echo "Сохранение $image в $filename..."
    docker save $image -o $filename
done < images-list.txt
```

---

### Шаг 3: Создание архива для передачи

**На локальной машине:**

```bash
# Создать архив со всеми образами
cd docker-images
tar -czf docker-images.tar.gz *.tar

# Проверить размер
ls -lh docker-images.tar.gz
```

---

### Шаг 4: Передача на сервер

**На локальной машине:**

```bash
# Передать архив на сервер через SCP
scp docker-images.tar.gz user@your-server:/tmp/

# Или через rsync (с прогрессом)
rsync -avz --progress docker-images.tar.gz user@your-server:/tmp/
```

**Альтернативные способы:**
- Использовать USB флешку
- Загрузить в облачное хранилище и скачать на сервере
- Использовать `sftp`

---

### Шаг 5: Загрузка образов на сервере

**На сервере:**

```bash
# 1. Распаковать архив
cd /tmp
tar -xzf docker-images.tar.gz

# 2. Загрузить образы в Docker
docker load -i backend.tar
docker load -i react-frontend.tar
docker load -i mcp-server.tar
docker load -i frontgres-16-alpine.tar
docker load -i nginx-alpine.tar

# Если используется frontend:
docker load -i frontend.tar

# 3. Проверить загруженные образы
docker images | grep -E "document-change-agent|postgres|nginx"
```

**Или загрузить все автоматически:**
```bash
cd /tmp
for file in *.tar; do
    echo "Загрузка $file..."
    docker load -i "$file"
done
```

---

### Шаг 6: Запуск на сервере

**На сервере:**

```bash
cd ~/document-change-agent

# Убедиться, что образы загружены
docker images | grep document-change-agent

# Запустить без сборки (используя существующие образы)
docker compose up -d

# Проверить статус
docker compose ps
docker compose logs --tail 50
```

---

## 🔧 Автоматизация (скрипт для локальной машины)

**Создайте скрипт `build-and-export.sh` на локальной машине:**

```bash
#!/bin/bash
# build-and-export.sh

set -e

echo "🔨 Сборка Docker образов..."

# Сборка всех образов
docker compose build

echo "📦 Сохранение образов..."

# Создать директорию
mkdir -p docker-images
cd docker-images

# Сохранить образы проекта
docker save document-change-agent-backend:latest -o backend.tar
docker save document-change-agent-react-frontend:latest -o react-frontend.tar
docker save document-change-agent-mcp-server:latest -o mcp-server.tar

# Сохранить базовые образы
docker save postgres:16-alpine -o postgres-16-alpine.tar
docker save nginx:alpine -o nginx-alpine.tar

echo "📚 Создание архива..."

# Создать архив
tar -czf docker-images.tar.gz *.tar

echo "✅ Готово! Архив: docker-images/docker-images.tar.gz"
echo "📤 Передайте на сервер: scp docker-images/docker-images.tar.gz user@server:/tmp/"
```

**Сделать исполняемым:**
```bash
chmod +x build-and-export.sh
./build-and-export.sh
```

---

## 🔧 Автоматизация (скрипт для сервера)

**Создайте скрипт `load-images.sh` на сервере:**

```bash
#!/bin/bash
# load-images.sh

set -e

echo "📥 Распаковка архива..."

cd /tmp
if [ ! -f docker-images.tar.gz ]; then
    echo "❌ Файл docker-images.tar.gz не найден в /tmp/"
    exit 1
fi

tar -xzf docker-images.tar.gz

echo "📦 Загрузка образов в Docker..."

for file in *.tar; do
    if [ -f "$file" ]; then
        echo "Загрузка $file..."
        docker load -i "$file"
    fi
done

echo "✅ Образы загружены!"
echo "📋 Список образов:"
docker images | grep -E "document-change-agent|postgres|nginx"

echo ""
echo "🚀 Теперь можно запустить:"
echo "   cd ~/document-change-agent"
echo "   docker compose up -d"
```

**Сделать исполняемым:**
```bash
chmod +x load-images.sh
./load-images.sh
```

---

## 📊 Размеры образов (примерные)

- `backend.tar` - ~500-800 MB
- `react-frontend.tar` - ~200-300 MB
- `mcp-server.tar` - ~300-500 MB
- `postgres-16-alpine.tar` - ~200-300 MB
- `nginx-alpine.tar` - ~50-100 MB

**Общий размер архива:** ~1.5-2 GB (сжатый ~500-800 MB)

---

## ⚠️ Важные замечания

### 1. Версии образов

Убедитесь, что на сервере используются те же версии:
```bash
# На локальной машине
docker images --format "{{.Repository}}:{{.Tag}}"

# На сервере после загрузки
docker images --format "{{.Repository}}:{{.Tag}}"
```

### 2. Обновление образов

При обновлении кода:
1. Пересобрать образы локально
2. Сохранить новые образы
3. Передать на сервер
4. Загрузить новые образы
5. Перезапустить контейнеры

### 3. Теги образов

Если используете теги (например, `:latest`), убедитесь, что они совпадают:
```bash
# На локальной машине
docker tag document-change-agent-backend:latest document-change-agent-backend:v1.0

# На сервере после загрузки
docker tag document-change-agent-backend:latest document-change-agent-backend:v1.0
```

### 4. docker-compose.yml

Убедитесь, что в `docker-compose.yml` не указаны `build:` секции, которые будут пытаться пересобрать образы:
```yaml
# Хорошо - использует существующий образ
services:
  backend:
    image: document-change-agent-backend:latest

# Плохо - будет пытаться собрать
services:
  backend:
    build:
      context: ./backend
```

---

## 🔄 Обновление образов

### Быстрое обновление одного образа:

**На локальной машине:**
```bash
# Пересобрать только backend
docker compose build backend

# Сохранить
docker save document-change-agent-backend:latest -o backend.tar

# Передать на сервер
scp backend.tar user@server:/tmp/

# На сервере
docker load -i /tmp/backend.tar
docker compose up -d backend
```

---

## 🎯 Рекомендуемый процесс

### Первоначальная установка:

1. **Локально:** Собрать все образы
2. **Локально:** Сохранить в архив
3. **Передать:** На сервер
4. **На сервере:** Загрузить образы
5. **На сервере:** Запустить `docker compose up -d`

### Обновление:

1. **Локально:** Пересобрать измененные образы
2. **Локально:** Сохранить только измененные
3. **Передать:** На сервер
4. **На сервере:** Загрузить новые образы
5. **На сервере:** Перезапустить: `docker compose down && docker compose up -d`

---

## ✅ Преимущества этого подхода

1. ✅ **Обход всех проблем с SSL/сетью** на сервере
2. ✅ **Быстрая установка** - не нужно ждать сборки
3. ✅ **Проверка локально** - можно протестировать перед передачей
4. ✅ **Повторное использование** - образы можно использовать на нескольких серверах
5. ✅ **Резервное копирование** - образы сохранены локально

---

**Версия:** 1.0  
**Дата:** 2025-11-24

