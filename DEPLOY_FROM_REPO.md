# 🚀 Развертывание проекта на удаленном сервере из репозитория

## Быстрое обновление проекта (если уже развернут)

Если проект уже развернут на сервере, выполните следующие команды:

```bash
# 1. Подключитесь к серверу
ssh user@your-server-ip

# 2. Перейдите в директорию проекта
cd ~/document-change-agent
# Или в ту директорию, где у вас находится проект

# 3. Остановите текущие контейнеры
docker compose down

# 4. Получите последние изменения из репозитория
git pull origin main

# 5. Пересоберите и запустите контейнеры
docker compose up --build -d

# 6. Проверьте статус
docker compose ps

# 7. Проверьте логи
docker compose logs -f backend
```

---

## Полное развертывание с нуля

### Шаг 1: Подключение к серверу

```bash
ssh user@your-server-ip
```

### Шаг 2: Установка необходимых компонентов

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Git (если не установлен)
sudo apt install -y git curl

# Установка Docker и Docker Compose (если не установлены)
sudo apt install -y \
    ca-certificates \
    gnupg \
    lsb-release

# Добавление GPG ключа Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Настройка репозитория Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установка Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверка установки
docker --version
docker compose version
```

### Шаг 3: Клонирование репозитория

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория
git clone https://github.com/marat-janalinov/document-change-agent.git

# Переход в директорию проекта
cd document-change-agent

# Проверка, что вы на ветке main и все изменения получены
git checkout main
git pull origin main
```

### Шаг 4: Настройка переменных окружения

```bash
# Создание файла .env
cat > .env << 'EOF'
# OpenAI API Key (ОБЯЗАТЕЛЬНО - замените на ваш ключ!)
OPENAI_API_KEY=sk-your-openai-api-key-here

# База данных PostgreSQL
POSTGRES_DB=document_agent
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here

# JWT Secret Key (сгенерируйте случайный ключ)
JWT_SECRET_KEY=your-very-secure-random-secret-key-min-32-chars

# MCP Server настройки
MCP_SERVER_HOST=mcp-server
MCP_SERVER_PORT=8000

# Data directory
DATA_DIR=/data

# PostgreSQL connection
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:your-secure-password-here@postgres:5432/document_agent
EOF

# Редактирование файла .env (замените значения на свои)
nano .env

# Генерация безопасного JWT Secret Key (выполните и скопируйте результат)
openssl rand -hex 32

# Установка прав на файл .env
chmod 600 .env
```

### Шаг 5: Создание необходимых директорий

```bash
# Создание директорий для данных
mkdir -p data/{uploads,outputs,backups,logs,prompts}

# Установка прав
sudo chmod -R 777 data/
```

### Шаг 6: Запуск проекта

```bash
# Первый запуск с пересборкой образов
docker compose up --build -d

# Проверка статуса контейнеров
docker compose ps

# Просмотр логов (для проверки запуска)
docker compose logs -f
```

### Шаг 7: Проверка работоспособности

```bash
# Проверка health check backend
curl http://localhost:8000/health

# Проверка frontend
curl http://localhost:8080

# Проверка логов каждого сервиса
docker compose logs backend
docker compose logs react-frontend
docker compose logs mcp-server
docker compose logs postgres
```

---

## Обновление проекта (получение последних изменений)

Когда в репозитории появляются новые изменения, выполните:

```bash
# 1. Перейдите в директорию проекта
cd ~/document-change-agent

# 2. Остановите контейнеры
docker compose down

# 3. Получите последние изменения из репозитория
git fetch origin
git pull origin main

# 4. Проверьте, что изменения получены
git log --oneline -5

# 5. Пересоберите образы (если изменился код)
docker compose build --no-cache

# 6. Запустите контейнеры
docker compose up -d

# 7. Проверьте статус
docker compose ps

# 8. Проверьте логи на наличие ошибок
docker compose logs -f --tail=100
```

### Если нужно сохранить данные при обновлении

```bash
# 1. Создайте резервную копию данных (опционально)
tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz data/

# 2. Выполните обычное обновление
cd ~/document-change-agent
docker compose down
git pull origin main
docker compose up --build -d

# Данные в папке data/ сохранятся автоматически (bind mounts)
```

---

## Настройка автоматического обновления (опционально)

Если хотите автоматически получать обновления:

```bash
# Создайте скрипт для автоматического обновления
cat > ~/update-project.sh << 'EOF'
#!/bin/bash
cd ~/document-change-agent

# Проверка изменений
git fetch origin
LOCAL=$(git rev-parse @)
REMOTE=$(git rev-parse @{u})

if [ $LOCAL != $REMOTE ]; then
    echo "Обнаружены новые изменения, обновляю проект..."
    docker compose down
    git pull origin main
    docker compose build --no-cache
    docker compose up -d
    echo "Проект обновлен!"
else
    echo "Изменений нет."
fi
EOF

# Установка прав на выполнение
chmod +x ~/update-project.sh

# Добавление в crontab для проверки каждые 6 часов
(crontab -l 2>/dev/null; echo "0 */6 * * * ~/update-project.sh >> ~/update-project.log 2>&1") | crontab -
```

---

## Проверка последних коммитов

Чтобы убедиться, что все изменения получены:

```bash
cd ~/document-change-agent

# Просмотр последних коммитов
git log --oneline -10

# Сравнение с удаленным репозиторием
git fetch origin
git log HEAD..origin/main --oneline

# Если есть вывод выше, значит есть неполученные изменения
# Выполните: git pull origin main
```

---

## Решение проблем

### Проблема: Git требует аутентификацию

**Решение:**
```bash
# Используйте SSH ключ вместо HTTPS
git remote set-url origin git@github.com:marat-janalinov/document-change-agent.git

# Или используйте Personal Access Token для HTTPS
git remote set-url origin https://YOUR_TOKEN@github.com/marat-janalinov/document-change-agent.git
```

### Проблема: Конфликты при git pull

**Решение:**
```bash
# Сохраните ваши локальные изменения
git stash

# Получите обновления
git pull origin main

# Примените ваши изменения обратно
git stash pop

# Разрешите конфликты, если они есть
# Затем закоммитьте
```

### Проблема: Контейнеры не запускаются после обновления

**Решение:**
```bash
# Просмотрите логи
docker compose logs

# Пересоберите образы без кэша
docker compose build --no-cache

# Перезапустите
docker compose up -d

# Если проблема сохраняется, проверьте изменения в docker-compose.yml
git diff HEAD~1 docker-compose.yml
```

### Проблема: Старые образы остаются в системе

**Решение:**
```bash
# Удаление неиспользуемых образов
docker image prune -a

# Удаление старых образов проекта
docker images | grep document-change-agent | awk '{print $3}' | xargs docker rmi -f
```

---

## Полезные команды для проверки

```bash
# Проверка текущей ветки
git branch

# Проверка последнего коммита
git log -1

# Проверка измененных файлов
git status

# Просмотр различий с удаленной веткой
git diff origin/main

# Проверка версии образа
docker images | grep document-change-agent

# Проверка запущенных контейнеров
docker compose ps

# Проверка использования ресурсов
docker stats
```

---

## Рекомендации

1. **Регулярные обновления**: Обновляйте проект регулярно, чтобы получать исправления и новые функции
2. **Резервное копирование**: Делайте резервные копии перед обновлением
3. **Тестирование**: После обновления проверяйте работоспособность всех функций
4. **Мониторинг логов**: Следите за логами после обновления на предмет ошибок

---

**Важно:** После каждого обновления проверяйте файл `.env` - возможно, добавились новые переменные окружения, которые нужно настроить.

