# 🔧 Исправление: Директория react/src/lib/ не была в git

**Дата:** 2025-11-24  
**Проблема:** Директория `react/src/lib/` отсутствовала на сервере, что вызывало ошибку сборки.

---

## 🔍 Причина проблемы

В `.gitignore` было правило `lib/`, которое игнорировало **все** директории с именем `lib`, включая `react/src/lib/`. 

В результате:
- ❌ Файлы из `react/src/lib/` не были добавлены в git
- ❌ Файлы не попали на сервер при `git pull`
- ❌ При сборке Vite не мог найти `/app/src/lib/utils`

---

## ✅ Выполненные исправления

### 1. Обновлен `.gitignore`

**Было:**
```
lib/
lib64/
```

**Стало:**
```
lib/
lib64/
# Исключение для react/src/lib (нужна для проекта)
!react/src/lib/
```

### 2. Добавлены файлы в git

Добавлены файлы из `react/src/lib/`:
- ✅ `utils.ts` - функция `cn()` для объединения классов
- ✅ `api.ts` - API клиент
- ✅ `translations.ts` - переводы
- ✅ `mockData.ts` - моковые данные

### 3. Закоммичены изменения

```bash
git add -f react/src/lib/
git commit -m "fix: Добавлена директория react/src/lib/ в git"
git push origin main
```

---

## 📋 Что нужно сделать на сервере

### Шаг 1: Получить обновления из git

```bash
cd ~/document-change-agent
git pull origin main
```

### Шаг 2: Проверить наличие директории

```bash
ls -la react/src/lib/
# Должны быть файлы:
# - utils.ts
# - api.ts
# - translations.ts
# - mockData.ts
```

### Шаг 3: Пересобрать контейнер

```bash
docker compose down
docker compose build --no-cache react-frontend
docker compose up -d
```

### Шаг 4: Проверить логи

```bash
docker compose logs react-frontend --tail 50
```

---

## ✅ Ожидаемый результат

После выполнения этих шагов:
- ✅ Директория `react/src/lib/` будет на сервере
- ✅ Файл `utils.ts` будет доступен
- ✅ Сборка пройдет успешно
- ✅ Приложение запустится без ошибок

---

## 🔍 Проверка на сервере

После `git pull` выполните:

```bash
# Проверка файлов
ls -la react/src/lib/
cat react/src/lib/utils.ts

# Должно показать:
# -rw-rw-r-- 1 user user 169 Nov 23 00:33 utils.ts
# import { clsx, type ClassValue } from "clsx";
# ...
```

---

**Версия:** 1.0  
**Дата:** 2025-11-24

