# 🔧 Исправление проблемы SSL с OpenAI API в Docker контейнере

**Дата:** 2025-11-24  
**Проблема:** `[SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: unable to get local issuer certificate`

---

## 🔍 Описание проблемы

При подключении к OpenAI API из Docker контейнера `backend` возникает ошибка SSL сертификата:

```
httpx.ConnectError: [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: unable to get local issuer certificate (_ssl.c:1016)
```

**Причина:** В контейнере отсутствуют или устарели SSL сертификаты для проверки HTTPS соединений.

**Почему локально работает:** На локальной машине системные сертификаты обновлены и актуальны.

---

## ✅ Решение

### 1. Использование `certifi` для SSL сертификатов

Вместо системных сертификатов используем `certifi` - Python пакет с актуальными SSL сертификатами.

**Изменения в `backend/parlant_agent.py`:**

```python
import certifi

# При инициализации httpx клиента:
cert_path = certifi.where()
self._openai_http_client = httpx.AsyncClient(
    timeout=300.0,
    verify=cert_path,  # Использовать сертификаты из certifi
)
```

### 2. Обновление Dockerfile

**Изменения в `backend/Dockerfile`:**

```dockerfile
# Обновление pip и certifi
RUN pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org --upgrade pip certifi

# Обновление certifi bundle (критично для работы с OpenAI API)
RUN python -m certifi || echo "certifi bundle уже обновлен"
```

---

## 🔧 Дополнительные опции

### Отключение проверки SSL (только для тестирования!)

Если проблема сохраняется, можно временно отключить проверку SSL через переменную окружения:

```bash
# В .env файле
OPENAI_VERIFY_SSL=false
```

**⚠️ ВНИМАНИЕ:** Это небезопасно! Используйте только для тестирования или если у вас есть другие механизмы безопасности.

---

## 📋 Проверка решения

### 1. Пересобрать контейнер

```bash
docker compose build backend
docker compose up -d backend
```

### 2. Проверить логи

```bash
docker compose logs backend | grep -i "SSL\|certifi\|OpenAI"
```

Должна появиться строка:
```
Использование SSL сертификатов из certifi: /usr/local/lib/python3.11/site-packages/certifi/cacert.pem
```

### 3. Проверить подключение к OpenAI API

```bash
# В контейнере
docker compose exec backend python -c "
import certifi
import httpx
import asyncio

async def test():
    cert_path = certifi.where()
    print(f'Certifi path: {cert_path}')
    client = httpx.AsyncClient(verify=cert_path, timeout=10.0)
    try:
        response = await client.get('https://api.openai.com/v1/')
        print(f'✅ Подключение успешно: HTTP {response.status_code}')
    except Exception as e:
        print(f'❌ Ошибка: {e}')
    finally:
        await client.aclose()

asyncio.run(test())
"
```

---

## 🎯 Почему это работает

1. **`certifi`** - это Python пакет, который содержит актуальный bundle SSL сертификатов
2. **`certifi.where()`** - возвращает путь к файлу с сертификатами
3. **`httpx.AsyncClient(verify=cert_path)`** - использует указанные сертификаты вместо системных
4. Обновление `certifi` в Dockerfile гарантирует актуальные сертификаты в образе

---

## 🔄 Альтернативные решения

### Вариант 1: Обновление системных сертификатов

```dockerfile
RUN apt-get update && \
    apt-get install -y ca-certificates && \
    update-ca-certificates
```

**Проблема:** Может не работать, если сертификаты на хосте устарели.

### Вариант 2: Копирование сертификатов с хоста

```dockerfile
COPY /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/
```

**Проблема:** Требует наличия актуальных сертификатов на хосте.

### Вариант 3: Использование certifi (рекомендуется) ✅

Используется в текущем решении - работает независимо от состояния хоста.

---

## 📝 Измененные файлы

1. **`backend/parlant_agent.py`**
   - Добавлен импорт `certifi`
   - Использование `certifi.where()` для SSL сертификатов
   - Логирование пути к сертификатам

2. **`backend/Dockerfile`**
   - Обновление `certifi` пакета
   - Запуск `python -m certifi` для обновления bundle

---

## 🚀 Развертывание

После изменений:

```bash
# 1. Пересобрать образ
docker compose build backend

# 2. Перезапустить контейнер
docker compose restart backend

# 3. Проверить работу
docker compose logs -f backend
```

---

## ⚠️ Важные замечания

1. **Безопасность:** Использование `certifi` безопасно и рекомендуется для Python приложений
2. **Обновление:** `certifi` обновляется регулярно, поэтому bundle всегда актуален
3. **Производительность:** Не влияет на производительность, только на безопасность соединений

---

**Версия:** 1.0  
**Дата:** 2025-11-24

