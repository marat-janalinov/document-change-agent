# 📁 Персистентность персональных директорий пользователей

**Дата:** 2025-11-24  
**Вопрос:** Являются ли персональные директории пользователей persistent и создаются ли они на хосте?

---

## ✅ Ответ

**Да, персональные директории пользователей являются persistent**, но они **НЕ создаются напрямую на хосте** как обычные папки. Они хранятся в **Docker named volumes**, которые управляются Docker и хранятся в специальном месте на хосте.

---

## 🔍 Текущая конфигурация

### Docker Volumes в `docker-compose.yml`

```yaml
volumes:
  # Постоянное хранилище для загруженных файлов
  # Включает персональные папки пользователей: /data/uploads/{username}/
  data-uploads:
    driver: local
```

### Монтирование в контейнере backend

```yaml
backend:
  volumes:
    - data-uploads:/data/uploads  # ← Персональные директории создаются здесь
    - data-outputs:/data/outputs
    - data-backups:/data/backups
    - data-logs:/data/logs
    - data-prompts:/data/prompts
```

---

## 📂 Где хранятся данные

### Named Volumes (текущая конфигурация)

**Linux:**
```
/var/lib/docker/volumes/document-change-agent_data-uploads/_data/
```

**macOS/Windows (Docker Desktop):**
```
~/Library/Containers/com.docker.docker/Data/vms/0/data/docker/volumes/document-change-agent_data-uploads/_data/
```

**Структура внутри volume:**
```
/data/uploads/
├── admin1/
│   ├── source/
│   └── changes/
├── operator1/
│   ├── source/
│   └── changes/
└── security1/
    ├── source/
    └── changes/
```

---

## ✅ Преимущества Named Volumes

1. **Персистентность** - данные сохраняются при перезапуске/удалении контейнеров
2. **Управление Docker** - автоматическое создание и управление
3. **Производительность** - оптимизированы для Docker
4. **Портативность** - легко переносятся между хостами через `docker volume export/import`

---

## ⚠️ Ограничения Named Volumes

1. **Нет прямого доступа с хоста** - нельзя просто открыть папку в файловом менеджере
2. **Сложный доступ** - нужно использовать Docker команды для доступа
3. **Резервное копирование** - требует специальных команд Docker

---

## 🔄 Альтернатива: Bind Mounts (доступ с хоста)

Если нужно, чтобы директории были **напрямую доступны на хосте**, можно использовать **bind mounts**:

### Изменение в `docker-compose.yml`:

```yaml
backend:
  volumes:
    # Вместо named volume используем bind mount
    - ./data/uploads:/data/uploads  # ← Директория на хосте
    - ./data/outputs:/data/outputs
    - ./data/backups:/data/backups
    - ./data/logs:/data/logs
    - ./data/prompts:/data/prompts
```

**Структура на хосте:**
```
./data/
├── uploads/
│   ├── admin1/
│   │   ├── source/
│   │   └── changes/
│   └── operator1/
│       ├── source/
│       └── changes/
├── outputs/
├── backups/
├── logs/
└── prompts/
```

### Преимущества Bind Mounts:

1. ✅ **Прямой доступ с хоста** - можно открыть папку в файловом менеджере
2. ✅ **Простое резервное копирование** - обычные команды `cp`, `rsync`, `tar`
3. ✅ **Легкий доступ для администратора** - не нужны Docker команды
4. ✅ **Видимость в IDE** - можно видеть файлы в редакторе

### Недостатки Bind Mounts:

1. ⚠️ **Зависимость от хоста** - путь должен существовать на хосте
2. ⚠️ **Права доступа** - могут быть проблемы с правами между хостом и контейнером
3. ⚠️ **Портативность** - привязаны к конкретному хосту

---

## 🛠️ Как проверить текущую конфигурацию

### 1. Проверить существующие volumes:

```bash
docker volume ls | grep data-uploads
```

### 2. Узнать путь к volume на хосте:

```bash
docker volume inspect document-change-agent_data-uploads
```

Или для всех volumes проекта:

```bash
docker volume inspect document-change-agent_data-uploads document-change-agent_data-outputs document-change-agent_data-backups
```

### 3. Проверить содержимое volume:

```bash
# Через контейнер
docker compose exec backend ls -la /data/uploads/

# Или через временный контейнер
docker run --rm -v document-change-agent_data-uploads:/data alpine ls -la /data/
```

### 4. Доступ к файлам в volume:

```bash
# Создать резервную копию
docker run --rm -v document-change-agent_data-uploads:/data -v $(pwd):/backup alpine tar czf /backup/uploads-backup.tar.gz -C /data .

# Восстановить из резервной копии
docker run --rm -v document-change-agent_data-uploads:/data -v $(pwd):/backup alpine tar xzf /backup/uploads-backup.tar.gz -C /data
```

---

## 🔄 Миграция с Named Volumes на Bind Mounts

Если нужно перейти на bind mounts:

### Шаг 1: Остановить контейнеры

```bash
docker compose down
```

### Шаг 2: Создать директории на хосте

```bash
mkdir -p ./data/{uploads,outputs,backups,logs,prompts}
chmod -R 777 ./data/
```

### Шаг 3: Скопировать данные из volume

```bash
# Создать временный контейнер для копирования
docker run --rm \
  -v document-change-agent_data-uploads:/source \
  -v $(pwd)/data/uploads:/dest \
  alpine sh -c "cp -a /source/. /dest/"
```

### Шаг 4: Изменить `docker-compose.yml`

Заменить named volumes на bind mounts:

```yaml
backend:
  volumes:
    - ./data/uploads:/data/uploads
    - ./data/outputs:/data/outputs
    - ./data/backups:/data/backups
    - ./data/logs:/data/logs
    - ./data/prompts:/data/prompts
```

### Шаг 5: Удалить старые volumes (опционально)

```bash
docker volume rm document-change-agent_data-uploads
```

### Шаг 6: Запустить контейнеры

```bash
docker compose up -d
```

---

## 📊 Сравнение подходов

| Характеристика | Named Volumes | Bind Mounts |
|----------------|---------------|-------------|
| Персистентность | ✅ Да | ✅ Да |
| Доступ с хоста | ❌ Нет (только через Docker) | ✅ Да (прямой доступ) |
| Управление Docker | ✅ Автоматическое | ⚠️ Ручное |
| Производительность | ✅ Оптимизирована | ⚠️ Зависит от хоста |
| Портативность | ✅ Высокая | ⚠️ Низкая |
| Резервное копирование | ⚠️ Через Docker | ✅ Обычными командами |
| Права доступа | ✅ Управляются Docker | ⚠️ Могут быть проблемы |

---

## ✅ Рекомендации

### Использовать Named Volumes, если:
- ✅ Нужна максимальная портативность
- ✅ Не нужен прямой доступ с хоста
- ✅ Хотите, чтобы Docker управлял хранилищем
- ✅ Работаете в production окружении

### Использовать Bind Mounts, если:
- ✅ Нужен прямой доступ к файлам с хоста
- ✅ Хотите простое резервное копирование
- ✅ Работаете в development окружении
- ✅ Нужен доступ к файлам из IDE

---

## 🔍 Текущий статус

**Текущая конфигурация:** Named Volumes  
**Персистентность:** ✅ Да, данные сохраняются  
**Доступ с хоста:** ❌ Нет, только через Docker команды  
**Расположение на хосте:** Docker-управляемое хранилище

---

## 📝 Итог

Персональные директории пользователей:
- ✅ **Являются persistent** - данные сохраняются при перезапуске контейнеров
- ❌ **НЕ создаются напрямую на хосте** - хранятся в Docker named volumes
- ✅ **Доступны через Docker команды** - можно получить доступ через контейнер или временные контейнеры
- ✅ **Сохраняются при удалении контейнеров** - volumes не удаляются автоматически

Если нужен прямой доступ с хоста, можно перейти на bind mounts, изменив конфигурацию в `docker-compose.yml`.

---

**Версия:** 1.0  
**Дата:** 2025-11-24

