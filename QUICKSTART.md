# 🚀 Быстрый запуск Document Change Agent

## Предварительные требования

1. **Docker & Docker Compose** - установлены и запущены
2. **OpenAI API Key** - получить на https://platform.openai.com/

## Шаг 1: Подготовка

```bash
# Клонировать проект
cd document-change-agent

# Создать .env файл
cp .env.example .env

# Отредактировать .env и добавить ваш OPENAI_API_KEY
nano .env
```

В файле `.env`:
```
OPENAI_API_KEY=sk-xxxx...
```

## Шаг 2: Запуск

```bash
# Запустить все сервисы
docker-compose up --build
```

Это запустит:
- **MCP Word Server** (порт 8000 внутри контейнера)
- **Backend** (порт 8000)
- **Frontend** (порт 3000)

## Шаг 3: Использование

1. Откройте браузер: http://localhost:3000

2. **Вариант A: Генерация тестовых файлов**
   - Нажмите "🎲 Сгенерировать тестовые файлы"
   - Система создаст пример документа с инструкциями

3. **Вариант B: Загрузка своих файлов**
   - Загрузите исходный .docx документ
   - Загрузите файл с инструкциями изменений

4. Нажмите "▶️ Начать обработку"

5. Наблюдайте real-time прогресс

6. Скачайте результаты

## Структура тестовых файлов

### Исходный документ (source.docx)
Содержит пронумерованные разделы:
- 1. Введение
- 2. Аутентификация
  - 2.1 Базовая аутентификация
  - 2.2 Token аутентификация
- 3. Endpoints
  - 3.1 Управление заказами
  - 3.2 Версия API
- И т.д.

### Файл с инструкциями (changes.docx)
Содержит изменения в естественном языке:
- "Измени в разделе 3.2 текст 'версия API v1.2' на 'версия API v2.0'"
- "Удали весь раздел '5. Устаревшие методы'"
- "Добавь новый раздел..."

## Проверка логов

```bash
# Логи backend
docker-compose logs -f backend

# Логи MCP сервера
docker-compose logs -f mcp-server

# Все логи
docker-compose logs -f
```

## Остановка

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить volumes
docker-compose down -v
```

## Troubleshooting

### Проблема: Backend не запускается

**Решение**: Проверьте, что OPENAI_API_KEY установлен корректно

```bash
docker-compose logs backend
```

### Проблема: MCP Server недоступен

**Решение**: Дождитесь полной инициализации (10-15 секунд)

```bash
docker-compose logs mcp-server
```

### Проблема: Веб-интерфейс не открывается

**Решение**: Проверьте, что порт 3000 свободен

```bash
# Проверка порта
lsof -i :3000

# Изменить порт в docker-compose.yml
ports:
  - "8080:80"  # вместо 3000:80
```

### Проблема: Файлы не сохраняются

**Решение**: Проверьте права доступа к ./data

```bash
# Создать директории с правильными правами
mkdir -p data/{uploads,outputs,backups}
chmod -R 777 data/
```

## Разработка

### Backend разработка (без Docker)

```bash
cd backend
pip install -r requirements.txt
export OPENAI_API_KEY=sk-xxxxx
uvicorn app:app --reload --host 0.0.0.0 --port 8000
```

### Frontend разработка (без Docker)

```bash
cd frontend
python -m http.server 3000
```

## Архитектура

```
User Browser (port 3000)
    ↓
Nginx Frontend
    ↓
FastAPI Backend (port 8000)
    ↓
Parlant Agent
    ↓
MCP Word Server
    ↓
Word Documents (.docx)
```

## API Endpoints

### Генерация тестовых файлов
```bash
curl -X POST http://localhost:8000/api/generate-test-files
```

### Запуск обработки
```bash
curl -X POST http://localhost:8000/api/process-documents \
  -H "Content-Type: application/json" \
  -d '{"source_filename": "source.docx", "changes_filename": "changes.docx"}'
```

### Статус обработки
```bash
curl http://localhost:8000/api/session/{session_id}/status
```

### Скачивание файла
```bash
curl http://localhost:8000/api/download/source.docx --output result.docx
```

## Следующие шаги

1. Попробуйте с тестовыми файлами
2. Создайте свои файлы с инструкциями
3. Изучите логи Parlant агента
4. Расширьте функционал (см. backend/parlant_agent.py)

## Поддержка

- GitHub Issues: [создать issue]
- Документация: README.md
- Parlant Docs: https://parlant.io/docs
