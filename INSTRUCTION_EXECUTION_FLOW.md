# 🔄 Процесс применения инструкций: от LLM до выполнения

## Содержание

1. [Общий процесс](#общий-процесс)
2. [Маппинг операций](#маппинг-операций)
3. [Детальный разбор каждой операции](#детальный-разбор-каждой-операции)
4. [Схемы выполнения](#схемы-выполнения)
5. [Примеры выполнения](#примеры-выполнения)

---

## Общий процесс

### Схема потока данных

```
┌─────────────────────────────────────────────────────────────┐
│ 1. LLM Парсинг инструкций                                    │
│    Вход: Текст из файла changes.docx                        │
│    Выход: JSON с массивом changes                           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Валидация и нумерация                                    │
│    - Проверка SUPPORTED_OPERATIONS                          │
│    - Добавление change_id (CHG-001, CHG-002, ...)          │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Последовательное выполнение изменений                    │
│    Для каждого change:                                      │
│    └─> _execute_change()                                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Маппинг операции на обработчик                          │
│    if operation == "REPLACE_TEXT":                          │
│        └─> _handle_replace_text()                          │
│    elif operation == "DELETE_PARAGRAPH":                    │
│        └─> _handle_delete_paragraph()                      │
│    ...                                                      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. Выполнение операции (MCP + python-docx)                 │
│    - Поиск текста через MCP                                │
│    - Модификация через python-docx или MCP                  │
│    - Добавление аннотаций                                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. Результат                                                │
│    - Статус выполнения (SUCCESS/FAILED)                     │
│    - Детали операции                                        │
│    - Информация об ошибках (если есть)                      │
└─────────────────────────────────────────────────────────────┘
```

---

## Маппинг операций

### Поддерживаемые операции

```python
SUPPORTED_OPERATIONS = {
    "REPLACE_TEXT",           # Замена текста
    "DELETE_PARAGRAPH",       # Удаление параграфа
    "INSERT_PARAGRAPH",       # Вставка параграфа
    "INSERT_SECTION",         # Вставка раздела
    "INSERT_TABLE",          # Вставка таблицы
    "ADD_COMMENT",           # Добавление комментария
    "REPLACE_POINT_TEXT",    # Замена всего пункта
}
```

### Таблица маппинга

| Операция LLM | Обработчик | MCP Инструменты | python-docx |
|--------------|------------|-----------------|-------------|
| `REPLACE_TEXT` | `_handle_replace_text()` | `find_text_in_document` | `Document()`, `para.text.replace()`, `doc.save()` |
| `REPLACE_POINT_TEXT` | `_handle_replace_point_text()` | `find_text_in_document` | `Document()`, удаление/вставка параграфов, `doc.save()` |
| `DELETE_PARAGRAPH` | `_handle_delete_paragraph()` | `find_text_in_document` | `Document()`, `_delete_paragraph()`, `doc.save()` |
| `INSERT_PARAGRAPH` | `_handle_insert_paragraph()` | `find_text_in_document` | `Document()`, `_insert_paragraph_after()`, `doc.save()` |
| `INSERT_SECTION` | `_handle_insert_section()` | `find_text_in_document`, `add_heading`, `add_paragraph` | `Document()`, `doc.save()` |
| `INSERT_TABLE` | `_handle_insert_table()` | `find_text_in_document`, `add_table` | - |
| `ADD_COMMENT` | `_handle_add_comment()` | `find_text_in_document`, `add_comment` | - |

### Код маппинга

```python
# backend/parlant_agent.py:590-603
async def _execute_change(self, filename: str, change: Dict[str, Any]):
    operation = change.get("operation", "").upper()
    
    if operation == "REPLACE_TEXT":
        details = await self._handle_replace_text(filename, change)
    elif operation == "REPLACE_POINT_TEXT":
        details = await self._handle_replace_point_text(filename, change)
    elif operation == "DELETE_PARAGRAPH":
        details = await self._handle_delete_paragraph(filename, change)
    elif operation == "INSERT_PARAGRAPH":
        details = await self._handle_insert_paragraph(filename, change)
    elif operation == "INSERT_SECTION":
        details = await self._handle_insert_section(filename, change)
    elif operation == "INSERT_TABLE":
        details = await self._handle_insert_table(filename, change)
    elif operation == "ADD_COMMENT":
        details = await self._handle_add_comment(filename, change)
```

---

## Детальный разбор каждой операции

### 1. REPLACE_TEXT (Замена текста)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-001",
  "operation": "REPLACE_TEXT",
  "description": "Заменить 'старый текст' на 'новый текст'",
  "target": {
    "text": "старый текст",
    "match_case": false,
    "replace_all": true
  },
  "payload": {
    "new_text": "новый текст"
  },
  "annotation": true
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
target_text = change["target"]["text"]        # "старый текст"
new_text = change["payload"]["new_text"]      # "новый текст"
match_case = change["target"].get("match_case", False)
replace_all = change["target"].get("replace_all", False)

# Шаг 2: Нормализация текста для поиска
normalized_target = " ".join(target_text.split())  # Удаление лишних пробелов

# Шаг 3: Поиск через MCP Server
matches = await mcp_client.find_text_in_document(
    filename,
    normalized_target,
    match_case=match_case,
)
# Возвращает: List[MCPTextMatch] с paragraph_index и text

# Шаг 4: Обработка результатов
if replace_all or len(matches) > 1:
    # Массовая замена через python-docx
    doc = Document(filename)
    for para in doc.paragraphs:
        if target_text in para.text:
            para.text = para.text.replace(target_text, new_text)
    doc.save(filename)
else:
    # Единичная замена
    doc = Document(filename)
    para = doc.paragraphs[matches[0].paragraph_index]
    para.text = para.text.replace(target_text, new_text)
    doc.save(filename)

# Шаг 5: Добавление аннотации (если annotation=true)
if change.get("annotation", True):
    await self._add_annotation(
        filename,
        paragraph_index,
        change,
        extra=f'"{target_text}" → "{new_text}"',
    )
```

#### Схема выполнения:

```
REPLACE_TEXT
    │
    ├─> Извлечение параметров (target.text, payload.new_text)
    │
    ├─> Нормализация текста
    │
    ├─> MCP: find_text_in_document()
    │   └─> Возвращает: List[MCPTextMatch]
    │
    ├─> Проверка количества совпадений
    │   ├─> Если replace_all или >1: Массовая замена
    │   └─> Если =1: Единичная замена
    │
    ├─> python-docx: Document(filename)
    │   ├─> Замена текста в параграфах
    │   └─> doc.save(filename)
    │
    └─> MCP: add_comment() (аннотация)
```

---

### 2. REPLACE_POINT_TEXT (Замена всего пункта)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-002",
  "operation": "REPLACE_POINT_TEXT",
  "description": "Пункт 36 изложить в новой редакции",
  "target": {
    "text": "36."
  },
  "payload": {
    "new_text": "36. Новый текст пункта полностью заменен."
  },
  "annotation": true
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
start_text = change["target"]["text"]      # "36."
new_text = change["payload"]["new_text"]  # "36. Новый текст..."

# Шаг 2: Поиск начала пункта через MCP
matches = await mcp_client.find_text_in_document(
    filename,
    start_text,
    match_case=False,
)

# Шаг 3: Определение границ пункта
start_idx = matches[0].paragraph_index
end_idx = self._find_section_end(doc, start_idx)  # Находит конец пункта

# Шаг 4: Удаление старого текста через python-docx
doc = Document(filename)
for idx in range(start_idx, end_idx):
    self._delete_paragraph(doc.paragraphs[start_idx])

# Шаг 5: Вставка нового текста
insert_after = doc.paragraphs[start_idx - 1]
self._insert_paragraph_after(insert_after, new_text)
doc.save(filename)

# Шаг 6: Добавление аннотации
await self._add_annotation(filename, start_idx, change, ...)
```

#### Схема выполнения:

```
REPLACE_POINT_TEXT
    │
    ├─> Извлечение параметров (target.text, payload.new_text)
    │
    ├─> MCP: find_text_in_document() - поиск начала пункта
    │
    ├─> Определение границ пункта (_find_section_end)
    │
    ├─> python-docx: Document(filename)
    │   ├─> Удаление старых параграфов (_delete_paragraph)
    │   └─> Вставка нового текста (_insert_paragraph_after)
    │
    ├─> doc.save(filename)
    │
    └─> MCP: add_comment() (аннотация)
```

---

### 3. DELETE_PARAGRAPH (Удаление параграфа)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-003",
  "operation": "DELETE_PARAGRAPH",
  "description": "Исключить пункт 30",
  "target": {
    "text": "30."
  },
  "annotation": true
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
text_to_remove = change["target"]["text"]  # "30."

# Шаг 2: Поиск через MCP
matches = await mcp_client.find_text_in_document(
    filename,
    text_to_remove,
    match_case=False,
)

# Шаг 3: Определение границ раздела для удаления
paragraph_index = matches[0].paragraph_index
start = paragraph_index
end = self._find_section_end(doc, paragraph_index)

# Шаг 4: Удаление через python-docx
doc = Document(filename)
for idx in range(start, end):
    para = doc.paragraphs[start]  # Индекс пересчитывается после удаления
    self._delete_paragraph(para)
doc.save(filename)

# Шаг 5: Добавление аннотации
await self._add_annotation(filename, start - 1, change, ...)
```

#### Схема выполнения:

```
DELETE_PARAGRAPH
    │
    ├─> Извлечение параметров (target.text)
    │
    ├─> MCP: find_text_in_document() - поиск текста
    │
    ├─> Определение границ раздела (_find_section_end)
    │
    ├─> python-docx: Document(filename)
    │   ├─> Удаление параграфов (_delete_paragraph)
    │   └─> doc.save(filename)
    │
    └─> MCP: add_comment() (аннотация)
```

---

### 4. INSERT_PARAGRAPH (Вставка параграфа)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-004",
  "operation": "INSERT_PARAGRAPH",
  "description": "Добавить после пункта 25",
  "target": {
    "after_text": "25."
  },
  "payload": {
    "text": "25.1. Новый подпункт",
    "style": "Normal"
  },
  "annotation": true
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
after_text = change["target"]["after_text"]    # "25."
new_paragraph = change["payload"]["text"]      # "25.1. Новый подпункт"
style = change["payload"].get("style")         # "Normal"

# Шаг 2: Поиск якоря через MCP
matches = await mcp_client.find_text_in_document(
    filename,
    after_text,
    match_case=False,
)

# Шаг 3: Вставка через python-docx
anchor_index = matches[0].paragraph_index
doc = Document(filename)
insert_after = doc.paragraphs[anchor_index]
self._insert_paragraph_after(insert_after, new_paragraph, style)
doc.save(filename)

# Шаг 4: Добавление аннотации
await self._add_annotation(filename, insert_position, change, ...)
```

#### Схема выполнения:

```
INSERT_PARAGRAPH
    │
    ├─> Извлечение параметров (target.after_text, payload.text)
    │
    ├─> MCP: find_text_in_document() - поиск якоря
    │
    ├─> python-docx: Document(filename)
    │   ├─> Вставка параграфа (_insert_paragraph_after)
    │   └─> doc.save(filename)
    │
    └─> MCP: add_comment() (аннотация)
```

---

### 5. INSERT_SECTION (Вставка раздела)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-005",
  "operation": "INSERT_SECTION",
  "description": "Добавить раздел после заголовка",
  "target": {
    "after_heading": "Раздел 2"
  },
  "payload": {
    "heading_text": "Раздел 2.1",
    "heading_level": 2,
    "paragraphs": [
      "Первый параграф раздела.",
      "Второй параграф раздела."
    ]
  },
  "annotation": true
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
after_heading = change["target"]["after_heading"]      # "Раздел 2"
heading_text = change["payload"]["heading_text"]       # "Раздел 2.1"
heading_level = change["payload"].get("heading_level", 2)
paragraphs = change["payload"].get("paragraphs", [])

# Шаг 2: Поиск якоря через MCP
matches = await mcp_client.find_text_in_document(
    filename,
    after_heading,
    match_case=False,
)

# Шаг 3: Вставка заголовка через MCP
anchor_index = matches[0].paragraph_index
await mcp_client.add_heading(
    filename,
    heading_text,
    level=heading_level,
    position=anchor_index + 1,
)

# Шаг 4: Вставка параграфов через MCP
for para_text in paragraphs:
    await mcp_client.add_paragraph(
        filename,
        para_text,
        position=anchor_index + 1,
    )

# Шаг 5: Добавление аннотации
await self._add_annotation(filename, anchor_index, change, ...)
```

#### Схема выполнения:

```
INSERT_SECTION
    │
    ├─> Извлечение параметров (target.after_heading, payload.heading_text, paragraphs)
    │
    ├─> MCP: find_text_in_document() - поиск якоря
    │
    ├─> MCP: add_heading() - вставка заголовка
    │
    ├─> MCP: add_paragraph() - вставка параграфов (для каждого)
    │
    └─> MCP: add_comment() (аннотация)
```

---

### 6. INSERT_TABLE (Вставка таблицы)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-006",
  "operation": "INSERT_TABLE",
  "description": "Вставить таблицу после пункта 36",
  "target": {
    "after_text": "36."
  },
  "payload": {
    "rows": [
      ["Колонка 1", "Колонка 2", "Колонка 3"],
      ["Значение 1", "Значение 2", "Значение 3"],
      ["Значение 4", "Значение 5", "Значение 6"]
    ],
    "columns": 3
  },
  "annotation": true
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
after_text = change["target"]["after_text"]  # "36."
rows = change["payload"]["rows"]              # [[...], [...], ...]
columns = change["payload"].get("columns")   # 3

# Шаг 2: Поиск якоря через MCP
matches = await mcp_client.find_text_in_document(
    filename,
    after_text,
    match_case=False,
)

# Шаг 3: Вставка таблицы через MCP
anchor_index = matches[0].paragraph_index
success = await mcp_client.add_table(
    filename,
    rows,
    position=anchor_index + 1,
)

# Шаг 4: Добавление аннотации
await self._add_annotation(filename, anchor_index, change, ...)
```

#### Схема выполнения:

```
INSERT_TABLE
    │
    ├─> Извлечение параметров (target.after_text, payload.rows, payload.columns)
    │
    ├─> MCP: find_text_in_document() - поиск якоря
    │
    ├─> MCP: add_table() - вставка таблицы
    │
    └─> MCP: add_comment() (аннотация)
```

---

### 7. ADD_COMMENT (Добавление комментария)

#### JSON структура от LLM:

```json
{
  "change_id": "CHG-007",
  "operation": "ADD_COMMENT",
  "description": "Добавить комментарий к пункту 10",
  "target": {
    "text": "10."
  },
  "payload": {
    "paragraph_hint": "10.",
    "comment_text": "Это комментарий к пункту 10"
  }
}
```

#### Процесс выполнения:

```python
# Шаг 1: Извлечение параметров
paragraph_hint = change["payload"]["paragraph_hint"]  # "10."
comment_text = change["payload"]["comment_text"]      # "Это комментарий..."

# Шаг 2: Поиск параграфа через MCP
matches = await mcp_client.find_text_in_document(
    filename,
    paragraph_hint,
    match_case=False,
)

# Шаг 3: Добавление комментария через MCP
paragraph_index = matches[0].paragraph_index
comment_id = await mcp_client.add_comment(
    filename,
    paragraph_index,
    comment_text,
)
```

#### Схема выполнения:

```
ADD_COMMENT
    │
    ├─> Извлечение параметров (payload.paragraph_hint, payload.comment_text)
    │
    ├─> MCP: find_text_in_document() - поиск параграфа
    │
    └─> MCP: add_comment() - добавление комментария
```

---

## Схемы выполнения

### Общая схема для всех операций

```
┌─────────────────────────────────────────────────────────────┐
│                    JSON от LLM                              │
│  {                                                           │
│    "operation": "REPLACE_TEXT",                             │
│    "target": {...},                                          │
│    "payload": {...}                                          │
│  }                                                           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              _execute_change()                               │
│  - Валидация операции                                        │
│  - Извлечение параметров                                    │
│  - Вызов соответствующего обработчика                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
┌──────────────┐            ┌──────────────────┐
│  _handle_*() │            │  MCP Server       │
│  (обработчик)│            │  - find_text      │
└──────┬───────┘            │  - add_*          │
       │                    └─────────┬──────────┘
       │                              │
       └──────────────┬───────────────┘
                      │
                      ▼
            ┌──────────────────┐
            │  python-docx     │
            │  - Document()    │
            │  - Модификация   │
            │  - doc.save()    │
            └─────────┬─────────┘
                      │
                      ▼
            ┌──────────────────┐
            │  Результат       │
            │  - success: true   │
            │  - details: {...} │
            └───────────────────┘
```

### Схема для операций с поиском

```
Все операции (кроме INSERT_SECTION без якоря):
    │
    ├─> Нормализация текста для поиска
    │   └─> " ".join(text.split())  # Удаление лишних пробелов
    │
    ├─> MCP: find_text_in_document()
    │   ├─> Поиск с нормализованным текстом
    │   ├─> Если не найдено: поиск с оригинальным текстом
    │   └─> Если не найдено: попытка вариантов (для пунктов)
    │
    ├─> Проверка результатов
    │   ├─> Если найдено: продолжение
    │   └─> Если не найдено: возврат ошибки TEXT_NOT_FOUND
    │
    └─> Использование paragraph_index для модификации
```

### Схема для операций с модификацией

```
Операции модификации (REPLACE, DELETE, INSERT):
    │
    ├─> python-docx: Document(filename)
    │   └─> Загрузка документа в память
    │
    ├─> Модификация через python-docx
    │   ├─> REPLACE_TEXT: para.text.replace()
    │   ├─> DELETE_PARAGRAPH: _delete_paragraph()
    │   ├─> INSERT_PARAGRAPH: _insert_paragraph_after()
    │   └─> REPLACE_POINT_TEXT: удаление + вставка
    │
    ├─> doc.save(filename)
    │   └─> Сохранение изменений на диск
    │
    └─> Добавление аннотации (если annotation=true)
        └─> MCP: add_comment()
```

---

## Примеры выполнения

### Пример 1: REPLACE_TEXT (массовая замена)

**Входной JSON:**
```json
{
  "change_id": "CHG-001",
  "operation": "REPLACE_TEXT",
  "target": {
    "text": "компания",
    "replace_all": true,
    "match_case": false
  },
  "payload": {
    "new_text": "организация"
  }
}
```

**Выполнение:**
1. `find_text_in_document("компания")` → найдено 5 вхождений
2. `Document(filename)` → загрузка документа
3. Проход по всем параграфам:
   - `para.text.replace("компания", "организация")` для каждого
4. `doc.save(filename)` → сохранение
5. `add_comment()` → аннотация

**Результат:**
```json
{
  "success": true,
  "replacements_count": 5,
  "affected_paragraphs": [2, 5, 8, 12, 15]
}
```

---

### Пример 2: DELETE_PARAGRAPH

**Входной JSON:**
```json
{
  "change_id": "CHG-002",
  "operation": "DELETE_PARAGRAPH",
  "target": {
    "text": "30."
  }
}
```

**Выполнение:**
1. `find_text_in_document("30.")` → найдено в параграфе 10
2. `_find_section_end(doc, 10)` → конец раздела в параграфе 12
3. `Document(filename)` → загрузка
4. Удаление параграфов 10, 11, 12:
   - `_delete_paragraph(doc.paragraphs[10])`
   - `_delete_paragraph(doc.paragraphs[10])` (индекс пересчитан)
   - `_delete_paragraph(doc.paragraphs[10])` (индекс пересчитан)
5. `doc.save(filename)` → сохранение
6. `add_comment()` → аннотация

**Результат:**
```json
{
  "success": true,
  "paragraph_index": 10
}
```

---

### Пример 3: INSERT_TABLE

**Входной JSON:**
```json
{
  "change_id": "CHG-003",
  "operation": "INSERT_TABLE",
  "target": {
    "after_text": "36."
  },
  "payload": {
    "rows": [
      ["№", "Наименование", "Количество"],
      ["1", "Товар А", "10"],
      ["2", "Товар Б", "20"]
    ],
    "columns": 3
  }
}
```

**Выполнение:**
1. `find_text_in_document("36.")` → найдено в параграфе 15
2. `add_table(filename, rows, position=16)` → вставка таблицы через MCP
3. `add_comment()` → аннотация

**Результат:**
```json
{
  "success": true,
  "paragraph_index": 15,
  "rows_count": 3,
  "columns_count": 3
}
```

---

## Ключевые моменты

### 1. Роль MCP Server

- ✅ **Поиск текста**: Все операции начинаются с поиска через `find_text_in_document()`
- ✅ **Чтение документов**: Извлечение текста из файлов инструкций
- ✅ **Специальные операции**: Вставка таблиц, заголовков, комментариев

### 2. Роль python-docx

- ✅ **Модификация документов**: Замена, удаление, вставка текста
- ✅ **Работа с параграфами**: Прямой доступ к структуре документа
- ✅ **Сохранение**: `doc.save()` для записи изменений

### 3. Гибридный подход

- ✅ **MCP для поиска**: Удобный поиск с контекстом и индексами
- ✅ **python-docx для модификации**: Полный контроль над изменениями
- ⚠️ **Потенциал для улучшения**: Можно использовать больше MCP инструментов

### 4. Обработка ошибок

- ✅ **Валидация параметров**: Проверка наличия обязательных полей
- ✅ **Поиск с вариантами**: Попытка найти текст с разными форматами
- ✅ **Детальные сообщения**: Информативные ошибки для пользователя

---

**Версия документа:** 1.0  
**Дата обновления:** 2025-11-24

