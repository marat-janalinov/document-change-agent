# 🔄 Активация промптов на удаленном сервере

После обновления кода из репозитория необходимо синхронизировать промпты на сервере.

## 📋 Варианты активации промптов

### Вариант 1: Автоматическая активация при перезапуске (РЕКОМЕНДУЕТСЯ)

Промпты автоматически копируются из `backend/prompts/` в `data/prompts/` при старте приложения через `init_prompts.py`.

**Шаги:**

1. **Обновить код на сервере:**
```bash
# На удаленном сервере
cd /path/to/document-change-agent
git pull origin main
```

2. **Перезапустить контейнеры:**
```bash
docker-compose down
docker-compose up --build -d
```

3. **Проверить логи:**
```bash
docker-compose logs backend | grep -i "промпт"
```

Вы должны увидеть:
```
✓ Промпты инициализированы
```

### Вариант 2: Ручное копирование промптов

Если нужно обновить промпты без перезапуска:

1. **Обновить код:**
```bash
cd /path/to/document-change-agent
git pull origin main
```

2. **Скопировать промпты вручную:**
```bash
# Копируем из репозитория в persistent volume
cp backend/prompts/*.md data/prompts/
```

3. **Перезапустить только backend контейнер:**
```bash
docker-compose restart backend
```

### Вариант 3: Через init_prompts.py вручную

1. **Войти в контейнер backend:**
```bash
docker-compose exec backend bash
```

2. **Запустить скрипт инициализации:**
```bash
python -c "from init_prompts import init_prompts; init_prompts()"
```

3. **Выйти из контейнера:**
```bash
exit
```

## ✅ Проверка активации промптов

### 1. Проверить версии файлов

На сервере проверьте размеры файлов промптов:

```bash
# На сервере
cd /path/to/document-change-agent
wc -l data/prompts/*.md
```

**Ожидаемые результаты:**
- `instruction_check_system.md`: **170+ строк**
- `instruction_check_user.md`: **164+ строк**

Если версии старые (меньше строк), значит промпты не обновились.

### 2. Проверить логи приложения

При старте backend вы должны увидеть в логах:

```bash
docker-compose logs backend | grep -i "prompt\|промпт"
```

**Ожидаемый вывод:**
```
✓ Промпты инициализированы
```

### 3. Проверить содержимое промпта

Проверьте, что в промпте есть новая структура:

```bash
# На сервере
grep -i "changes" data/prompts/instruction_check_system.md | head -5
```

Должны увидеть упоминания о структуре `{"changes": [...]}`.

## 🔍 Диагностика проблем

### Проблема: Промпты не обновились

**Причина:** Файлы в `data/prompts/` уже существуют, и `init_prompts.py` не перезаписывает существующие файлы.

**Решение:**
```bash
# Удалить старые промпты
rm data/prompts/*.md

# Перезапустить контейнер
docker-compose restart backend

# Проверить, что промпты скопировались
ls -la data/prompts/
```

### Проблема: Файлы промптов не найдены

**Причина:** Директория `data/prompts/` не существует или не смонтирована.

**Решение:**
```bash
# Создать директорию
mkdir -p data/prompts

# Проверить, что она смонтирована в docker-compose.yml
grep -A 5 "prompts" docker-compose.yml

# Перезапустить контейнеры
docker-compose down && docker-compose up -d
```

## 📝 Проверка работы LLM после обновления

После обновления промптов проверьте работу:

1. **Загрузить тестовый файл с инструкциями** через веб-интерфейс
2. **Нажать "Проверить инструкции"**
3. **Проверить логи:**
```bash
docker-compose logs -f backend | grep -i "LLM\|JSON\|changes"
```

**Ожидаемое поведение:**
- LLM должен возвращать JSON в формате `{"changes": [...]}`
- Должны распознаваться ВСЕ инструкции из файла
- В логах должно быть: `✅ Найдено X изменений` (где X > 1, если в файле несколько инструкций)

## 🚨 Важные замечания

1. **Persistent Volume:** Файлы промптов хранятся в `data/prompts/`, который является persistent volume. Если этот volume не создан или не подключен, промпты будут загружаться из `backend/prompts/` внутри контейнера.

2. **Приоритет загрузки:** Приложение сначала ищет промпты в `data/prompts/`, затем в `backend/prompts/`.

3. **Без перезапуска:** Изменения промптов применяются сразу после обновления файлов - перезапуск не обязателен, но рекомендуется для уверенности.

4. **Версии промптов:** Всегда проверяйте версии промптов после обновления, сравнивая количество строк с репозиторием.

## 📞 Быстрая команда для обновления

Если нужно быстро обновить промпты:

```bash
# На сервере
cd /path/to/document-change-agent && \
git pull origin main && \
rm -f data/prompts/*.md && \
docker-compose restart backend && \
sleep 5 && \
docker-compose logs backend | grep -i "промпт\|prompt"
```

Эта команда:
1. Обновляет код из репозитория
2. Удаляет старые промпты
3. Перезапускает backend (который скопирует новые промпты)
4. Показывает логи с информацией о промптах

