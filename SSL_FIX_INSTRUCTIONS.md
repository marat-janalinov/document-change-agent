# 🔧 Инструкция по исправлению SSL проблемы на сервере

**Дата:** 2025-11-24  
**Проблема:** `[SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed`

---

## ✅ Быстрое решение (рекомендуется)

### Шаг 1: Добавить переменную окружения в `.env`

На сервере откройте файл `.env` и добавьте (или измените) строку:

```bash
OPENAI_VERIFY_SSL=false
```

### Шаг 2: Перезапустить контейнер

```bash
docker compose restart backend
```

### Шаг 3: Проверить логи

```bash
docker compose logs backend | grep -i "SSL\|certifi"
```

Должна появиться строка:
```
Проверка SSL отключена (OPENAI_VERIFY_SSL=false или не установлен)
```

---

## 🔄 Полное решение (пересборка контейнера)

Если хотите попробовать использовать certifi:

### Шаг 1: Получить обновления

```bash
cd ~/document-change-agent
git pull origin main
```

### Шаг 2: Пересобрать контейнер

```bash
docker compose down
docker compose build --no-cache backend
docker compose up -d backend
```

### Шаг 3: Проверить установку certifi

```bash
docker compose exec backend python -c "import certifi; print(certifi.where())"
```

### Шаг 4: Если certifi работает, включить SSL проверку

В `.env` файле:
```bash
OPENAI_VERIFY_SSL=true
```

Перезапустить:
```bash
docker compose restart backend
```

---

## ⚠️ Важно

**По умолчанию SSL проверка отключена** (`OPENAI_VERIFY_SSL=false`), чтобы система работала на серверах с проблемами SSL.

Это **небезопасно**, но необходимо для работы. Если у вас есть другие механизмы безопасности (VPN, прокси), это приемлемо.

---

## 🔍 Проверка работы

После применения изменений попробуйте:

1. Проверить инструкции в файле
2. Применить изменения к документу

Если ошибка SSL больше не появляется, значит проблема решена.

---

## 📝 Что было изменено

1. **По умолчанию SSL проверка отключена** - система будет работать даже без certifi
2. **Улучшена обработка ошибок** - если certifi не работает, автоматически отключается SSL
3. **Добавлено логирование** - в логах видно, используется ли SSL проверка

---

**Версия:** 1.0  
**Дата:** 2025-11-24

