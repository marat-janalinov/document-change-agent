# 📁 Доработка функционала создания персональных директорий пользователей

**Дата:** 2025-11-24  
**Задача:** При создании пользователя через панель администратора автоматически создавать персональные директории с поддиректориями `source` и `changes`.

---

## ✅ Выполненные изменения

### 1. Добавлена функция `ensure_user_directories()` в `backend/app.py`

**Новая функция:**
```python
def ensure_user_directories(username: str) -> str:
    """
    Создание всех необходимых персональных директорий пользователя:
    - основная директория пользователя
    - поддиректория source (для исходных файлов)
    - поддиректория changes (для файлов с инструкциями)
    
    Возвращает путь к основной директории пользователя.
    """
    user_dir = ensure_user_directory(username)
    
    # Создаем поддиректории source и changes
    source_dir = os.path.join(user_dir, "source")
    changes_dir = os.path.join(user_dir, "changes")
    
    os.makedirs(source_dir, exist_ok=True)
    os.makedirs(changes_dir, exist_ok=True)
    
    logger.info(f"Созданы директории для пользователя {username}: {user_dir}, {source_dir}, {changes_dir}")
    
    return user_dir
```

### 2. Обновлена функция создания пользователя в `backend/auth_routes.py`

**Было:**
```python
# Создание персональной директории пользователя
try:
    import os
    import re
    DATA_DIR = os.getenv("DATA_DIR", "/data")
    UPLOADS_DIR = os.path.join(DATA_DIR, "uploads")
    safe_username = re.sub(r'[^A-Za-z0-9_-]', '_', new_user.username)
    user_dir = os.path.join(UPLOADS_DIR, safe_username)
    os.makedirs(user_dir, exist_ok=True)
except Exception as e:
    logging.getLogger(__name__).warning(f"Не удалось создать директорию для пользователя {new_user.username}: {e}")
```

**Стало:**
```python
# Создание всех необходимых персональных директорий пользователя
try:
    import os
    import re
    import logging
    logger = logging.getLogger(__name__)
    
    DATA_DIR = os.getenv("DATA_DIR", "/data")
    UPLOADS_DIR = os.path.join(DATA_DIR, "uploads")
    safe_username = re.sub(r'[^A-Za-z0-9_-]', '_', new_user.username)
    user_dir = os.path.join(UPLOADS_DIR, safe_username)
    
    # Создаем основную директорию пользователя
    os.makedirs(user_dir, exist_ok=True)
    
    # Создаем поддиректории source и changes
    source_dir = os.path.join(user_dir, "source")
    changes_dir = os.path.join(user_dir, "changes")
    os.makedirs(source_dir, exist_ok=True)
    os.makedirs(changes_dir, exist_ok=True)
    
    logger.info(f"Созданы директории для пользователя {new_user.username}: {user_dir}, {source_dir}, {changes_dir}")
except Exception as e:
    logging.getLogger(__name__).warning(f"Не удалось создать директории для пользователя {new_user.username}: {e}")
```

### 3. Обновлен скрипт `backend/create_users.py`

**Было:**
```python
# Создание персональной директории пользователя
try:
    import os
    import re
    DATA_DIR = os.getenv("DATA_DIR", "/data")
    UPLOADS_DIR = os.path.join(DATA_DIR, "uploads")
    safe_username = re.sub(r'[^A-Za-z0-9_-]', '_', user.username)
    user_dir = os.path.join(UPLOADS_DIR, safe_username)
    os.makedirs(user_dir, exist_ok=True)
    print(f"✓ Создана директория для пользователя {user_data['username']}")
except Exception as e:
    print(f"⚠ Не удалось создать директорию для {user_data['username']}: {e}")
```

**Стало:**
```python
# Создание всех необходимых персональных директорий пользователя
try:
    import os
    import re
    DATA_DIR = os.getenv("DATA_DIR", "/data")
    UPLOADS_DIR = os.path.join(DATA_DIR, "uploads")
    safe_username = re.sub(r'[^A-Za-z0-9_-]', '_', user.username)
    user_dir = os.path.join(UPLOADS_DIR, safe_username)
    
    # Создаем основную директорию пользователя
    os.makedirs(user_dir, exist_ok=True)
    
    # Создаем поддиректории source и changes
    source_dir = os.path.join(user_dir, "source")
    changes_dir = os.path.join(user_dir, "changes")
    os.makedirs(source_dir, exist_ok=True)
    os.makedirs(changes_dir, exist_ok=True)
    
    print(f"✓ Созданы директории для пользователя {user_data['username']}: {user_dir}, {source_dir}, {changes_dir}")
except Exception as e:
    print(f"⚠ Не удалось создать директории для {user_data['username']}: {e}")
```

### 4. Обновлен код в `backend/app.py` для использования новой функции

**В `startup_event`:**
- Заменен вызов `ensure_user_directory()` на `ensure_user_directories()` для всех существующих пользователей
- Убрано дублирование создания поддиректорий

**В `upload_file`:**
- Заменен вызов `ensure_user_directory()` на `ensure_user_directories()`
- Убрано дублирование создания поддиректорий `source` и `changes`

**В `process_documents_task`:**
- Заменен вызов `get_user_directory()` на `ensure_user_directories()` для создания резервных копий
- Убрано дублирование создания поддиректории `source`

---

## 📂 Структура директорий

После создания пользователя создается следующая структура:

```
/data/uploads/
└── {username}/
    ├── source/      # Для исходных файлов
    └── changes/     # Для файлов с инструкциями
```

**Пример:**
```
/data/uploads/
├── admin1/
│   ├── source/
│   └── changes/
├── operator1/
│   ├── source/
│   └── changes/
└── security1/
    ├── source/
    └── changes/
```

---

## 🔍 Где создаются директории

1. **При создании пользователя через админ-панель** (`/api/auth/users` POST)
   - Автоматически создаются все необходимые директории

2. **При запуске приложения** (`startup_event`)
   - Проверяются и создаются директории для всех существующих пользователей

3. **При загрузке файла** (`/api/upload` POST)
   - Проверяются и создаются директории, если их нет

4. **При создании резервной копии** (`process_documents_task`)
   - Проверяются и создаются директории перед созданием резервной копии

---

## ✅ Преимущества

1. **Автоматизация** - директории создаются автоматически при создании пользователя
2. **Консистентность** - одинаковая структура для всех пользователей
3. **Надежность** - директории создаются в нескольких местах для подстраховки
4. **Логирование** - все операции создания директорий логируются

---

## 🧪 Проверка работы

1. **Создайте нового пользователя через админ-панель:**
   - Перейдите в раздел "Управление пользователями"
   - Нажмите "Создать пользователя"
   - Заполните форму и сохраните

2. **Проверьте создание директорий:**
   ```bash
   docker compose exec backend ls -la /data/uploads/
   docker compose exec backend ls -la /data/uploads/{username}/
   docker compose exec backend ls -la /data/uploads/{username}/source/
   docker compose exec backend ls -la /data/uploads/{username}/changes/
   ```

3. **Проверьте логи:**
   ```bash
   docker compose logs backend | grep "Созданы директории"
   ```

---

## ⚠️ Важные замечания

1. **Безопасность имен** - имена пользователей очищаются от опасных символов с помощью регулярного выражения `[^A-Za-z0-9_-]`

2. **Обработка ошибок** - если создание директорий не удалось, это не прерывает создание пользователя, но логируется предупреждение

3. **Существующие пользователи** - при запуске приложения директории создаются для всех существующих пользователей, если их еще нет

4. **Идемпотентность** - функция `ensure_user_directories()` использует `exist_ok=True`, поэтому можно вызывать ее многократно без ошибок

---

## ✅ Итоговый статус

- ✅ Добавлена функция `ensure_user_directories()` для создания всех необходимых директорий
- ✅ Обновлена функция создания пользователя в админ-панели
- ✅ Обновлен скрипт `create_users.py`
- ✅ Обновлены все места в коде, где создаются директории пользователей
- ✅ Добавлено логирование операций создания директорий
- ✅ Убрано дублирование кода создания поддиректорий

**Все изменения готовы к использованию.**

---

**Версия:** 1.0  
**Дата:** 2025-11-24

