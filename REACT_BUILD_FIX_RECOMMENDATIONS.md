# 🔧 Рекомендации по исправлению ошибки сборки React на Ubuntu

**Дата:** 2025-11-24  
**Ошибка:** `Could not load /app/src/lib/utils (imported by src/components/ui/tooltip.tsx): ENOENT: no such file or directory`

---

## 🔍 Анализ проблемы

Ошибка возникает при сборке React приложения на сервере Ubuntu, хотя локально все работает. Vite не может найти файл `/app/src/lib/utils` при импорте из `tooltip.tsx`.

**Возможные причины:**
1. **Case sensitivity** - Linux чувствителен к регистру файлов
2. **Проблема с разрешением модулей** - Vite не может разрешить путь `@/lib/utils`
3. **Проблема с копированием файлов в Docker** - файл не копируется или копируется неправильно
4. **Проблема с расширением файла** - Vite ищет файл без расширения

---

## ✅ Рекомендации по исправлению

### Рекомендация 1: Проверить наличие файла в Docker контейнере

**На сервере Ubuntu выполните:**

```bash
# Войдите в контейнер builder во время сборки
docker compose build react-frontend --no-cache

# Или проверьте файлы после копирования
docker compose run --rm react-frontend sh -c "ls -la /app/src/lib/"
```

**Ожидаемый результат:** Должен быть файл `utils.ts`

**Если файла нет:**
- Проверьте `.dockerignore` - возможно файл исключен
- Проверьте, что файл добавлен в git и копируется в Docker

---

### Рекомендация 2: Проверить регистр файлов

**На сервере Ubuntu (Linux чувствителен к регистру):**

```bash
# Проверьте точное имя файла
cd react/src/lib
ls -la | grep -i utils

# Убедитесь, что файл называется именно utils.ts (не Utils.ts или UTILS.ts)
```

**Если проблема в регистре:**
- Переименуйте файл в правильный регистр
- Убедитесь, что все импорты используют правильный регистр

---

### Рекомендация 3: Проверить .dockerignore

**Проверьте файл `react/.dockerignore`:**

Убедитесь, что файл `utils.ts` не исключен из копирования:

```bash
# На сервере
cat react/.dockerignore

# Если там есть что-то вроде:
# *.ts
# src/lib/*
# то это может быть проблемой
```

**Решение:** Убедитесь, что `src/lib/utils.ts` не исключен в `.dockerignore`.

---

### Рекомендация 4: Явно указать расширение в импортах (если возможно)

**Проблема:** Vite пытается загрузить `/app/src/lib/utils` без расширения.

**Проверьте `vite.config.ts`:**

Убедитесь, что в конфигурации правильно настроено разрешение модулей:

```typescript
resolve: {
  alias: {
    "@": path.resolve(__dirname, "./src"),
  },
  extensions: ['.ts', '.tsx', '.js', '.jsx'], // Убедитесь, что это есть
}
```

---

### Рекомендация 5: Проверить порядок копирования в Dockerfile

**Проблема:** Файлы могут копироваться в неправильном порядке.

**Текущий Dockerfile:**
```dockerfile
COPY package*.json ./
COPY bun.lockb* ./
RUN npm install
COPY . .  # ← Копирует все файлы
RUN npm run build
```

**Рекомендация:** Убедитесь, что `COPY . .` копирует все файлы, включая `src/lib/utils.ts`.

**Проверка:**
```bash
# На сервере, перед сборкой
cd react
find src/lib -name "utils.ts" -ls
```

---

### Рекомендация 6: Очистить кэш и пересобрать

**На сервере Ubuntu:**

```bash
# Остановить контейнеры
docker compose down

# Удалить образы и кэш
docker compose build --no-cache react-frontend

# Или полностью очистить
docker system prune -a

# Пересобрать
docker compose build react-frontend
docker compose up -d
```

---

### Рекомендация 7: Проверить права доступа к файлам

**На сервере Ubuntu:**

```bash
# Проверьте права доступа
ls -la react/src/lib/utils.ts

# Должно быть что-то вроде:
# -rw-r--r-- 1 user user 169 Nov 23 00:33 utils.ts

# Если права неправильные, исправьте:
chmod 644 react/src/lib/utils.ts
```

---

### Рекомендация 8: Проверить содержимое файла utils.ts

**Убедитесь, что файл содержит правильный код:**

```typescript
// react/src/lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Проверка на сервере:**
```bash
cat react/src/lib/utils.ts
```

---

### Рекомендация 9: Проверить зависимости в package.json

**Убедитесь, что установлены необходимые зависимости:**

```bash
# На сервере
cd react
cat package.json | grep -E "clsx|tailwind-merge"

# Должно быть:
# "clsx": "^версия"
# "tailwind-merge": "^версия"
```

**Если зависимостей нет:**
```bash
npm install clsx tailwind-merge
```

---

### Рекомендация 10: Проверить tsconfig.json

**Убедитесь, что пути настроены правильно:**

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

**Проверка на сервере:**
```bash
cat react/tsconfig.json
```

---

## 🔧 Пошаговое решение (рекомендуемый порядок)

### Шаг 1: Проверка файла на сервере

```bash
# SSH на сервер
ssh user@your-server

# Перейти в директорию проекта
cd /path/to/document-change-agent

# Проверить наличие файла
ls -la react/src/lib/utils.ts
cat react/src/lib/utils.ts
```

### Шаг 2: Проверка .dockerignore

```bash
cat react/.dockerignore
# Убедитесь, что там нет исключений для utils.ts или src/lib/*
```

### Шаг 3: Очистка и пересборка

```bash
# Остановить контейнеры
docker compose down

# Удалить старые образы
docker rmi document-change-agent-react-frontend 2>/dev/null || true

# Пересобрать без кэша
docker compose build --no-cache react-frontend

# Запустить
docker compose up -d
```

### Шаг 4: Проверка логов

```bash
# Смотреть логи сборки
docker compose logs react-frontend

# Если ошибка повторяется, проверьте файлы внутри контейнера
docker compose run --rm react-frontend sh -c "ls -la /app/src/lib/"
```

---

## 🎯 Наиболее вероятные причины

1. **Файл не копируется в Docker** - проверьте `.dockerignore`
2. **Проблема с регистром** - Linux чувствителен к регистру
3. **Проблема с кэшем Docker** - нужна пересборка без кэша
4. **Проблема с разрешением путей** - проверьте `vite.config.ts`

---

## 📝 Чеклист для диагностики

- [ ] Файл `react/src/lib/utils.ts` существует на сервере
- [ ] Файл имеет правильный регистр (`utils.ts`, не `Utils.ts`)
- [ ] Файл не исключен в `.dockerignore`
- [ ] Файл добавлен в git и присутствует в репозитории
- [ ] Зависимости `clsx` и `tailwind-merge` установлены
- [ ] `vite.config.ts` правильно настроен с алиасом `@`
- [ ] `tsconfig.json` правильно настроен с путями
- [ ] Docker кэш очищен и пересборка выполнена без кэша
- [ ] Права доступа к файлу корректны (644)

---

## ⚠️ Важные замечания

1. **Не меняйте код** - проблема скорее всего в окружении или конфигурации
2. **Проверьте на сервере** - локально может работать, но на сервере нет
3. **Используйте `--no-cache`** - при пересборке для исключения проблем с кэшем
4. **Проверьте логи** - детальные логи сборки помогут найти проблему

---

## 🔍 Дополнительная диагностика

Если проблема не решается, выполните на сервере:

```bash
# Полная диагностика
cd /path/to/document-change-agent/react

# 1. Проверка файла
echo "=== Проверка файла ==="
ls -la src/lib/utils.ts
file src/lib/utils.ts

# 2. Проверка .dockerignore
echo "=== Проверка .dockerignore ==="
cat .dockerignore | grep -E "utils|lib|src" || echo "Нет исключений"

# 3. Проверка package.json
echo "=== Проверка зависимостей ==="
grep -E "clsx|tailwind-merge" package.json

# 4. Проверка vite.config.ts
echo "=== Проверка vite.config.ts ==="
cat vite.config.ts | grep -A 5 "resolve"

# 5. Тестовая сборка локально на сервере (без Docker)
echo "=== Тестовая сборка ==="
npm install
npm run build 2>&1 | head -50
```

---

**Версия:** 1.0  
**Дата:** 2025-11-24

