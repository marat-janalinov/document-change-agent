# 📋 Отчет по исправлению отображения файлов

**Дата:** 2025-11-24  
**Проблема:** Файлы в персональных директориях не отображаются во всех разделах

---

## ✅ Выполненные исправления

### 1. Добавлена загрузка .env в `backend/app.py`
- ✅ Добавлен импорт `dotenv` и `Path`
- ✅ Добавлена загрузка `.env` файла в начале модуля
- ✅ Теперь все переменные окружения корректно читаются

### 2. Добавлена нормализация `DATA_DIR`
- ✅ Относительные пути преобразуются в абсолютные
- ✅ Добавлено логирование для отладки
- ✅ Поддержка как локальной разработки, так и Docker

### 3. Улучшена функция `list_files`
- ✅ Добавлено детальное логирование всех этапов
- ✅ Логирование путей, проверка существования директорий
- ✅ Автоматическое создание поддиректорий при необходимости

### 4. Исправлен `DATA_DIR` в .env
- ✅ Изменено с `./data` на `/data` для Docker
- ✅ Это гарантирует правильную работу в контейнере

---

## 🔍 Диагностика проблемы

### Проверка структуры директорий

**Локально:**
```bash
ls -la data/uploads/operator1/source/
ls -la data/uploads/operator1/changes/
```

**В Docker контейнере:**
```bash
docker compose exec backend ls -la /data/uploads/operator1/source/
docker compose exec backend ls -la /data/uploads/operator1/changes/
```

### Проверка логов

После перезапуска backend проверьте логи:
```bash
docker compose logs -f backend | grep -E "DATA_DIR|UPLOADS_DIR|список файлов|Директория пользователя|Найдено.*файлов"
```

**Ожидаемые логи при запросе списка файлов:**
```
INFO - DATA_DIR из переменной окружения: '/data'
INFO - DATA_DIR используется как абсолютный путь: /data
INFO - Итоговые пути: DATA_DIR=/data, UPLOADS_DIR=/data/uploads
INFO - Запрос списка файлов для пользователя operator1, file_type=source
INFO - DATA_DIR: /data
INFO - UPLOADS_DIR: /data/uploads
INFO - Директория пользователя: /data/uploads/operator1
INFO - Абсолютный путь к директории пользователя: /data/uploads/operator1
INFO - Существует ли директория пользователя: True
INFO - Поиск файлов в подпапке: /data/uploads/operator1/source
INFO - Абсолютный путь: /data/uploads/operator1/source
INFO - Существует ли директория: True
INFO - Все файлы в директории /data/uploads/operator1/source: ['source.docx']
INFO - Найдено 1 файлов в папке source (исключены backup файлы): ['source.docx']
```

---

## 🔧 Дополнительные проверки

### 1. Проверка volume в Docker

Убедитесь, что volume правильно подключен:
```bash
docker compose exec backend mount | grep data
```

Должно быть:
```
/data/uploads on /var/lib/docker/volumes/... type ...
```

### 2. Проверка прав доступа

```bash
docker compose exec backend ls -la /data/uploads/
docker compose exec backend ls -la /data/uploads/operator1/
```

### 3. Проверка API запроса

В браузере откройте DevTools → Network и проверьте:
- Запрос к `/api/files?file_type=source` должен возвращать файлы
- Запрос к `/api/files?file_type=changes` должен возвращать файлы
- Проверьте заголовок `Authorization: Bearer ...`

### 4. Проверка аутентификации

Убедитесь, что пользователь правильно аутентифицирован:
- В логах должно быть имя пользователя
- Проверьте токен в localStorage браузера

---

## 📝 Структура директорий

### Ожидаемая структура:

```
/data/uploads/
  └── {username}/
      ├── source/      # Исходные файлы
      │   └── *.docx
      └── changes/     # Файлы с инструкциями
          └── *.docx
```

### Пример для пользователя `operator1`:

```
/data/uploads/operator1/
  ├── source/
  │   └── source.docx
  └── changes/
      └── changes.docx
```

---

## 🚀 Следующие шаги

1. **Перезапустите backend:**
   ```bash
   docker compose restart backend
   ```

2. **Проверьте логи:**
   ```bash
   docker compose logs -f backend
   ```

3. **Проверьте файлы в контейнере:**
   ```bash
   docker compose exec backend find /data/uploads -name "*.docx" -type f
   ```

4. **Проверьте API напрямую:**
   ```bash
   # Получите токен из браузера (localStorage.getItem('auth_token'))
   curl -H "Authorization: Bearer YOUR_TOKEN" \
        "http://localhost:8000/api/files?file_type=source"
   ```

---

## ⚠️ Возможные проблемы

### Проблема 1: Файлы не видны в контейнере

**Причина:** Volume не подключен или файлы в другой директории

**Решение:**
```bash
# Проверьте volume
docker compose exec backend ls -la /data/uploads/

# Если директория пустая, проверьте локальную директорию
ls -la data/uploads/
```

### Проблема 2: Ошибка аутентификации

**Причина:** Токен недействителен или отсутствует

**Решение:**
- Войдите заново в систему
- Проверьте токен в localStorage браузера
- Проверьте логи backend на наличие ошибок 401

### Проблема 3: Неправильный file_type

**Причина:** Запрос идет без параметра `file_type` или с неправильным значением

**Решение:**
- Проверьте Network tab в DevTools
- Убедитесь, что запрос содержит `?file_type=source` или `?file_type=changes`

---

## ✅ Итоговый статус

- ✅ Загрузка .env добавлена в `app.py`
- ✅ Нормализация DATA_DIR реализована
- ✅ Детальное логирование добавлено
- ✅ DATA_DIR исправлен в .env (`/data` вместо `./data`)
- ✅ Функция `list_files` улучшена

**Все исправления готовы к использованию.**

После перезапуска backend файлы должны отображаться во всех разделах.

---

**Версия:** 1.0  
**Дата:** 2025-11-24

