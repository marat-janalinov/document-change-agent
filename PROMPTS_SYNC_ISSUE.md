# ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА: Несинхронизированные промпты

## 🔍 Обнаруженная проблема

**Файлы промптов в `data/prompts/` отличаются от версий в `backend/prompts/`!**

Это объясняет различия в поведении между локальной и удаленной средой.

## 📊 Сравнение версий

### `instruction_check_system.md`:

| Расположение | Размер | Дата изменения | MD5 хеш |
|-------------|--------|----------------|---------|
| `backend/prompts/` | 11,397 байт | 28 ноября | `78f2fa861eb3eee449a747dff1bc0fb8` |
| `data/prompts/` (старая) | 3,231 байт | 24 ноября | `f2fbf291a04b8132c6c77cbee301ba6d` |

**Разница:** Старая версия в 3.5 раза меньше! Содержит только базовые инструкции.

### `instruction_check_user.md`:

| Расположение | Размер | Дата изменения | MD5 хеш |
|-------------|--------|----------------|---------|
| `backend/prompts/` | 7,566 байт | 28 ноября | `34a1d8aebc77c158ce90e6fdfe053c93` |
| `data/prompts/` (старая) | 10,229 байт | 24 ноября | `583d97ebb496a76c3af3a1a2530d6221` |

**Разница:** Разные версии с разными алгоритмами парсинга.

## 🔧 Что было изменено в новых версиях промптов

### Новый `instruction_check_system.md` включает:

1. **Детальный алгоритм анализа:**
   - Определение области применения (локальные vs глобальные изменения)
   - Определение типа элемента (параграф, таблица, документ)
   - Выбор операции

2. **Правила разбиения сложных инструкций:**
   - Обработка инструкций с несколькими операциями
   - Сохранение порядка выполнения

3. **Критические правила:**
   - Правильное извлечение `target.text` из кавычек
   - Запрет использования номеров пунктов как `target.text`
   - Специальные правила для работы с таблицами

4. **Требования к JSON:**
   - Обязательная структура с массивом `changes`
   - Обработка одного объекта изменения

### Старая версия содержала только:
- Базовые инструкции по поиску инструкций
- Простые требования к JSON
- Отсутствие детальных правил парсинга

## ❌ Последствия использования старых промптов

1. **LLM возвращает неправильную структуру JSON:**
   - Один объект вместо массива `changes`
   - Неправильное извлечение `target.text`
   - Отсутствие обработки сложных инструкций

2. **Неправильное распознавание инструкций:**
   - Пропуск некоторых инструкций
   - Неправильный парсинг таблиц
   - Ошибки в определении области применения

3. **Различия в поведении:**
   - Локально работает (новые промпты)
   - На сервере не работает (старые промпты)

## ✅ Решение

### Шаг 1: Синхронизация локально

```bash
# Убедитесь, что используете актуальные промпты
cd ~/Downloads/document-change-agent

# Синхронизируйте промпты
cp backend/prompts/*.md data/prompts/

# Проверьте синхронизацию
md5sum backend/prompts/*.md data/prompts/*.md
# Хеши должны совпадать!
```

### Шаг 2: Синхронизация на сервере

```bash
# Вариант A: Через rsync (рекомендуется)
rsync -avz backend/prompts/*.md user@server:~/document-change-agent/data/prompts/

# Вариант B: Через SCP
scp backend/prompts/*.md user@server:~/document-change-agent/data/prompts/

# Вариант C: Удалить старые и перезапустить (промпты скопируются автоматически)
ssh user@server
cd ~/document-change-agent
rm data/prompts/*.md
docker compose restart backend
# Промпты скопируются из backend/prompts/ при запуске
```

### Шаг 3: Проверка на сервере

```bash
# На сервере проверьте синхронизацию
ssh user@server
cd ~/document-change-agent

# Сравните хеши
md5sum backend/prompts/*.md data/prompts/*.md

# Или проверьте размеры файлов
ls -lh backend/prompts/ data/prompts/

# Перезапустите backend для применения изменений
docker compose restart backend
```

## 🔄 Автоматическая синхронизация

При запуске backend скрипт `init_prompts.py` автоматически копирует промпты из `backend/prompts/` в `data/prompts/`, **НО ТОЛЬКО ЕСЛИ ФАЙЛОВ ЕЩЕ НЕТ** в `data/prompts/`.

Если файлы уже существуют (как на вашем сервере), они **НЕ ПЕРЕЗАПИСЫВАЮТСЯ** автоматически.

**Поэтому важно вручную синхронизировать промпты!**

## 📝 Рекомендации

1. **Всегда синхронизируйте промпты** при развертывании на новом сервере
2. **Проверяйте версии промптов** после обновления кода
3. **Используйте MD5 хеши** для проверки совпадения версий
4. **Добавьте проверку промптов** в процесс деплоя

## 🚨 Важно

- Промпты в `backend/prompts/` - это **источник истины** (версионируются в Git)
- Промпты в `data/prompts/` - это **runtime версии** (используются при работе)
- При изменениях промптов **ОБЯЗАТЕЛЬНО** синхронизируйте оба места
- **Отличия в промптах = разные результаты парсинга инструкций!**

